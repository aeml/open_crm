# Security Surface Inventory

Audit date: 2026-07-21

Registered route count: `247`

Registered route digest: `06487ac14f7ff7589bfdf8eaadeced3f2ff65a415fecf8ec22b5129f888521b7`

This is the Phase 0 map for every HTTP route and continuously running background operation. The route count and digest are derived from all production `http.ServeMux` registrations in `apps/api/internal/app`; a package-wide backend scan fails when that set changes so a split, new, or renamed route cannot silently bypass this review or the hosted-write-policy coverage check.

`tenant-isolation-matrix.md` complements this route policy with a separately
digest-gated set of 25 real-PostgreSQL negative-path tests for every promoted
Phase 2 service family. Route registration proves that a boundary was reviewed;
the matrix identifies the database acceptance that proves the service behavior.

## Policy vocabulary

| Term | Executable meaning |
| --- | --- |
| Public | No session is required. Tenant context, when needed, is resolved from an opaque public ID, slug, or signed/random token rather than caller input. |
| Member | `requireOrgMember`: owner, admin, member, and viewer may proceed. |
| Writer | `requireOrgWriter`: owner, admin, and member may proceed; viewer receives `403`. |
| Admin | `requireOrgAdmin`: owner and admin may proceed; member/viewer receive `403`. |
| Session tenant | The handler passes the organization ID from the server-side session. Resource services must include it in SQL predicates; callers cannot select an organization ID. |
| Token tenant | A public identifier or token resolves the organization/resource in the service. Invalid or cross-resource tokens do not expose tenant data. |
| Browser CORS | Credentialed cross-origin browser traffic is reflected only for configured exact origins. Unsafe requests still pass the CSRF/origin boundary; allowed request headers are `Content-Type`, `Authorization`, and the non-secret `Idempotency-Key` used by portable-export, quote-finalization/delivery/approval, and threaded-reply requests. |
| Hosted write policy | Only a runtime configured with Stripe is managed. Every registered private managed-tenant mutation is classified from its exact method/route and enforces subscription writability after authentication and its normal role check. Canceled, expired-trial, unpaid, paused, incomplete, and incomplete-expired tenants receive `402`; unreadable configured policy receives `503`. Reads/exports, workspace offboarding export requests, billing recovery, durable-job replay, own profile/preferences, notification acknowledgement, invitation revocation, public compliance/provider callbacks, and the Stripe webhook are explicit exceptions. Invitation resend remains a normal managed write. Public lead capture and replayed non-billing work use the same managed boundary. Fake/self-hosted mode reports `unmanaged`, exposes unlimited local usage, and skips hosted lifecycle/limit checks across private routes, public leads, and workers so stale hosted fields cannot restrict core data. |
| Plan create | The route checks both subscription writability and the named seat/record limit. Configured limit reads fail closed; a nil billing service remains compatible with isolated tests/callers. Current named resources are seats, contacts, and deals. |
| Request telemetry | Request ID and structured completion logs plus bounded-route Prometheus counters/histograms are emitted by global middleware. Metrics never label tenants, record IDs, addresses, or request bodies. |
| Shared public abuse budget | A static route scope and one-way SHA-256 client digest identify an atomic PostgreSQL fixed window; no raw address is retained. All API processes consume the same bounded counter, return the remaining window in `Retry-After`, and fail closed with `503 RATE_LIMIT_UNAVAILABLE` when enforcement cannot be verified. Expired rows are removed in bounded opportunistic batches. |

Unless a row says otherwise, private routes have no endpoint-specific rate limit; they rely on the authenticated session, the cross-cutting hosted write policy for tenant mutations, CSRF/origin checks for unsafe methods, the 1 MiB JSON body limit (2 MiB import upload), server timeouts, and global request telemetry. The policy registration test sends every classified route through an inactive tenant and fails if any route misses the authorization/billing boundary. This is a map of current behavior, not a claim that every control is sufficient.

## HTTP routes

Selectors below are mutually scoped by method and path. Braced names are Go `ServeMux` path parameters; “family” covers the listed collection and item paths only, not arbitrary future routes.

| Surface and registered selectors | Authentication / role | Tenant boundary | Plan entitlement | Abuse controls | Observability | Current test evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `POST /auth/login`, `POST /auth/setup-password` | Public credential or one-time-token flow | Organization is selected only after credential validation | None | Shared public abuse budget: 10/client/minute; bounded JSON | Request plus bounded abuse-decision telemetry | `auth_test.go`, `users_test.go`, route-family rate-limit tests, and concurrent PostgreSQL acceptance |
| `POST /auth/bootstrap`, `POST /auth/verify-email`, `POST /auth/resend-verification` | Public signup and one-time owner-verification flow; bootstrap creates no session, verification creates the first owner session | Provisioning writes one organization/owner/default pipeline transactionally; a SHA-256 request fingerprint and advisory-locked idempotency key prevent duplicate or conflicting tenants. Verification resolves the organization only from a 256-bit expiring token | Trial begins only after successful verification | Shared public abuse budget: bootstrap is limited to 3/client/hour; verify and resend each use 10/client/minute. Resend is generic, active-unverified-user only, and has a persisted one-minute recipient cooldown. JSON is bounded; correct passwords cannot log in before verification | Request/provider/abuse-decision telemetry plus transactional provisioning and verification audit events | Handler mapping/rate-limit tests, concurrent shared-store acceptance, onboarding disposable-PostgreSQL lifecycle acceptance, email-provider template tests, and the clean browser verified-signup journey |
| `POST /auth/request-password-reset`, `POST /auth/reset-password` | Public enumeration-resistant email request and one-time-token completion | Requests select only a verified user with an active membership by normalized email; completion resolves the global user only from a 256-bit token digest and writes no caller-supplied tenant ID. The password change, every-session invalidation, token consumption, and one audit event per workspace membership commit atomically | None; account recovery remains available regardless of hosted workspace write state | Request is 5/client/hour plus a persisted five-minute recipient cooldown; failed delivery is immediately retryable with token rotation. Completion is 10/client/minute. Responses for eligible, unknown, unverified, disabled, cooled-down, and provider-failed requests are the same generic non-cacheable `202`; raw tokens are never persisted/logged or sent as browser referrers, and the UI replaces the token-bearing history entry after completion. A fake-provider local link is exposed only when `GO_ENV` is explicitly `development` or `test`; production/Postmark responses never expose it | Request/provider/abuse-decision telemetry, aggregate outstanding/stale/failed delivery gauges and alerts, delivery state, and transactional per-membership audit evidence | Handler/error/cookie and route-budget tests; disposable-PostgreSQL eligibility, cooldown, expiry, replay, delivery recovery, session invalidation, audit, and credential acceptance; email template, focused UI, WCAG, and clean browser multi-device recovery coverage |
| `GET /auth/me` | Valid session | Session tenant | None | Cookie only | Request telemetry | `auth_test.go` covers valid, stale, and missing sessions |
| `POST /auth/logout` | Session is used when present; stale/missing cookies are safely cleared | Session token only | None | CSRF/origin | Request telemetry | `auth_test.go` |
| `GET /healthz`, `GET /readyz`, fallback `/` | Public | None; readiness checks database connectivity | None | Read-only, no dedicated limiter | Request telemetry | `app_test.go`, `readyz_test.go` |
| `GET /metrics` | A configured bearer token of at least 32 characters; route is hidden with `404` when unconfigured | No tenant data; queue, notification, password-recovery, system-email feedback, public-lead outcomes, and lead-review health are aggregated across all tenants | None | Read-only, constant-time token comparison, loopback-bound API port in deployment Compose | Bounded route/status/latency, database, aggregate queue, provider outcome, public abuse decisions, lead challenge/accept/replay/reject/error outcomes, lead-review state/age, password-recovery/system-email delivery health, and backup/restore evidence | `metrics_test.go`, telemetry package tests |
| `GET /api/users` | Member | Session tenant | None | Private defaults | Request telemetry; responses expose explicit pending/expired/accepted/revoked invitation state and expiry, never a raw token | Handler and disposable-PostgreSQL invitation tests verify organization propagation and state |
| `POST /api/users` | Admin | Session tenant | Plan create: `seats` | Private defaults; invite delivery starts only after the membership commits, reports `sent` or recoverable `failed`, and exposes a setup link only for the fake provider outside production; raw tokens are never serialized | Request/provider telemetry plus audit event | `users_test.go`, `email_test.go`, `billing_test.go`, `role_permissions_test.go` |
| `POST /api/users/{userID}/invitation/resend`; `DELETE /api/users/{userID}/invitation` | Admin | Session tenant plus locked organization membership and global user; foreign targets are indistinguishable from missing | Resend is a normal managed write; security-reducing revoke remains available in hosted read-only mode | Resend rotates the 256-bit token before provider delivery so every older link fails; provider failure is explicit and safely retryable. Revoke atomically removes the token, disables the seat, invalidates sessions, unassigns active work, and is idempotent. Accepted users cannot enter either flow; raw tokens are digest-only at rest and never serialized, and fake setup links are development/test-only | Request/provider telemetry plus transactional resend/revoke audit events | Handler role/error/secret-hiding tests, hosted-policy classification, disposable-PostgreSQL expiry/rotation/replay/revoke/reactivation/cross-tenant/login acceptance, focused UI, and clean browser lifecycle journey |
| `PATCH /api/users/{userID}/role` | Admin | Session tenant plus organization-scoped target | None | Private defaults | Request telemetry plus audit event | `users_test.go`; service constraints protect last-owner behavior |
| `PATCH /api/users/{userID}/status` | Admin | Session tenant plus locked organization membership; cross-tenant targets and replacements are rejected. Quote delivery preparation and claims lock/revalidate active membership before intent/delivery rows, matching lifecycle lock order | None | Private defaults; self-deactivation and last-active-owner removal are rejected; transition is idempotent; the whole serializable transaction retries four times on PostgreSQL serialization/deadlock conflicts | Request telemetry plus transactional lifecycle audit; follower subscriptions, pending member jobs, and quote-delivery effects are quiesced. A late intent cannot commit after deactivation, prepared quote sends fail before the provider boundary, and already claimed sends become explicit `uncertain` rather than being retried | Handler forbidden/error tests, deterministic concurrent session-touch, preparation/deactivation race, and prepared/in-flight quote-delivery PostgreSQL acceptance, plus the browser activation/deactivation/reactivation journey |
| `GET`, `PUT`, `DELETE /api/users/{userID}/email-account` | Admin | Session tenant plus organization-scoped target user | None | Private defaults | Request telemetry; credential values are write-only/masked | `user_email_admin_test.go` covers roles and organization propagation |
| `PATCH /api/me/profile`; `GET`, `PATCH /api/me/preferences` | Member, including viewer for own settings | Session tenant and session user | None | Private defaults | Request telemetry; profile mutation is audited | `business_profile_test.go`/profile handler tests |
| `GET /api/me/sessions`; `DELETE /api/me/sessions/{sessionID}`, `/others` | Member, including viewer for own account; revocation remains available during hosted suspension | The authenticated global user ID comes only from the current server-side session. Listing joins only that user's active workspace memberships; revocation locks the user/current session and rejects foreign or missing IDs identically | None; account-recovery exception | Private defaults plus `no-store`; unsafe methods use CSRF/origin checks; the current session cannot be ended through the management routes; expired sessions are omitted/pruned; responses expose workspace/timestamps but no token, address, or fingerprint | Request telemetry; transactional revocation audit in every current/historical membership | Handler safe-error/scope tests, focused loading/confirmation/retry UI tests, disposable-PostgreSQL ownership/current-session/expiry/idempotency/audit acceptance, forced login/reset race, WCAG scan, and clean browser manual-plus-reset invalidation journey |
| `GET`, `PUT`, `DELETE /api/me/email-account`; `GET /api/me/email-sync/status`; `POST /api/me/email-sync/check`, `/run`, `/oauth/{provider}/start`; `GET /api/me/email-sync/oauth/{provider}/callback` | Member, including viewer for own mailbox | Session tenant and session user; OAuth state is signed, expiring, and exact-user/provider validated; mailbox feedback additionally requires an exact same-tenant/mailbox outbound RFC message and non-conflicting recipient | None | Private defaults; passwords/tokens are encrypted and write-only; actual delegated scopes determine send readiness; refresh use is advisory-lock serialized per tenant/user; provider sends are not automatically retried; ordinary and temporary DSN mail is ignored; unmatched feedback fails closed | Bounded request, provider send/refresh, sync, and durable-job telemetry plus aggregate customer bounce/complaint/unapplied gauges without credential/tenant/recipient labels; bounded 400-day feedback retention | `user_email_sync_test.go`, OAuth/account/send handler tests, exact Gmail/Graph raw-MIME HTTP tests, DSN/ARF parser tests, and disposable-PostgreSQL encryption/concurrency/feedback/tenant tests |
| `GET /api/audit-events`; `GET /api/audit-events/export.csv` | Admin | Session tenant; list and CSV queries always bind the organization from the session | None; audit recovery remains readable during hosted suspension | Private defaults; the CSV refuses more than 10,000 matching rows instead of truncating, neutralizes spreadsheet formulas, and is non-cacheable | Request telemetry plus an append-only audit event for every successful CSV download; top-level secret-like metadata keys are rejected in PostgreSQL | Handler list/filter/role/overflow/export tests, disposable-PostgreSQL mixed-JSON/tenant/formula/immutability/overflow acceptance, static producer inventory, and the clean browser download journey |
| `GET`, `POST /api/workspace-exports`; `GET /api/workspace-exports/{exportID}/download` | Admin; request/download remain available while hosted writes are suspended | Session tenant plus tenant-scoped request/artifact lookup; foreign IDs are indistinguishable from missing | Offboarding recovery exception, no paid-plan dependency | Required 255-character-bounded idempotency key; one queued job per tenant/key; repeatable-read snapshot; 200 MiB uncompressed and 50 MiB compressed ceilings; ZIP is `no-store`, SHA-256 labeled, and expires after seven days | Request telemetry; transactional request/ready/download audit evidence; durable job retry/dead-letter/Operations visibility | Handler recovery/role/header/failure tests plus disposable-PostgreSQL idempotency, schema-coverage, redaction, cross-tenant, ZIP, checksum, audit, and expiry acceptance; billing UI test covers suspended self-service access |
| `GET /api/admin/background-jobs`; `POST /api/admin/background-jobs/{jobID}/replay`, `/resolve-sequence-delivery` | Admin | Session tenant plus tenant-scoped job claim/update | None | Private defaults; replay is state-checked/idempotent | Request telemetry, audit event on recovery, structured worker logs, operator UI | `background_jobs_test.go`, job/sequence PostgreSQL tests |
| `GET /api/billing/plans` | Member | Plans are global/static | None | Private defaults | Request telemetry | `billing_test.go` |
| `GET /api/billing/entitlements` | Member | Session tenant | None | Private defaults | Request telemetry | `billing_test.go` |
| `GET /api/billing/usage` | Member | Session tenant; every counter query and every dynamically discovered storage source uses the session organization. Retained snapshots are unique per organization and period | No commercial limit or feature dependency; the idempotent evidence upsert does not create new quotas. Stripe uses the exact stored provider period when available; fake/self-hosted mode falls back to the current UTC month | Private defaults; one repeatable-read source reconciliation per request, bounded serialization retries, a 256-table discovery ceiling, and safe PostgreSQL identifier quoting | Request telemetry plus an upserted observed-at snapshot of counts, source period/basis, and source-table count | Handler membership/error tests and disposable-PostgreSQL period, source-filter, concurrent repeatability, retained-snapshot, and cross-tenant acceptance |
| `GET /api/billing/invoices` | Admin | Session tenant; fixed newest-first query filters the durable provider invoice ledger by the session organization | No feature or writable-subscription dependency; payment evidence remains readable during suspension/cancellation | Private defaults; maximum 25 returned rows and only absolute HTTPS document links without URL userinfo leave the service boundary | Request telemetry; signed webhook/provider reconciliation remains the durable source | Handler role/error tests, disposable-PostgreSQL link-safety/cross-tenant acceptance, app-level Stripe sandbox journey, and focused billing UI test |
| `POST /api/billing/change-plan` | Admin | Session tenant | Fake-provider plan mutation for self-host/development; Stripe mode rejects direct activation and requires Checkout | Private defaults | Request telemetry plus audit event | `billing_test.go` covers roles and errors |
| `POST /api/billing/checkout-session`, `POST /api/billing/portal-session` | Admin | Session tenant; server supplies organization/customer metadata and owner email | Paid plans require a configured Stripe price; no plan/status changes occur from the browser redirect | Private defaults; durable request fingerprint and Stripe idempotency key for Checkout | Request/provider telemetry; durable checkout attempt ledger | Handler tests, Stripe HTTP-contract and disposable-PostgreSQL lifecycle acceptance, plus the isolated Stripe-mode browser journey through both destinations and redirect non-authority |
| `POST /api/billing/webhooks/stripe` | Public; raw-body Stripe HMAC signature and five-minute replay window required | Signed provider references resolve one organization; mismatched metadata/customer/subscription fail closed | Webhook subscription/invoice state is authoritative | 1 MiB raw body; shared 120/client/minute budget; provider event ID and payload hash are durable/idempotent | Request/abuse-decision telemetry, webhook receipt/failure ledger, invoice records, and subscription/payment audit events | Route-family budget test, signature/HTTP handler tests, duplicate/tamper/ordering/cross-tenant/subscription/invoice PostgreSQL acceptance, and isolated browser-level signed-event replay through activation, dunning, recovery, and cancellation |
| `POST /api/email/webhooks/postmark` | Public provider callback; hidden with `404` until a dedicated username and at least 32-character password are configured, then constant-time Basic Auth is required | Open CRM's versioned metadata identifies the purpose, user, optional organization, and an attempt-specific 256-bit delivery key. The raw key must match its stored digest, normalized recipient, active one-time flow, and locked tenant membership before state changes. Shared-stream events without Open CRM's marker are acknowledged and ignored | None; delivery/compliance feedback remains authoritative during hosted suspension | 64 KiB raw body; authenticated callbacks use the shared 120/client/minute budget. Provider event ID plus exact payload hash is durable/idempotent; conflicting reuse receives `403` so Postmark stops retrying | Request/abuse-decision telemetry; aggregate bounce, complaint, and unapplied gauges/alerts; tenant audit evidence; a secret-safe 400-day feedback ledger that stores no recipient, message body, subject, or provider payload | Authentication/body/rate/retry handler tests plus freshly migrated PostgreSQL acceptance for exact-attempt application, all three identity-mail purposes, duplicate/tamper behavior, cross-tenant/stale-key denial, complaint suppression/audit, privacy, metrics, and retention |
| Email templates/snippets: collection `GET`, `POST`; item `PATCH`, `DELETE`; `GET /api/email-templates/merge-fields` | Member read; Writer mutation | Session tenant and tenant-scoped item SQL | None | Private defaults | Request telemetry | `email_templates_test.go` includes handler/role/error paths; newer item paths need broader explicit cross-tenant cases |
| Product catalog: collection `GET`, `POST`; item `PATCH`, `DELETE` | Member read; Writer mutation | Session tenant and tenant-scoped item SQL | None | Private defaults | Request telemetry | `product_catalog_test.go`; explicit cross-tenant handler coverage is incomplete |
| Lead capture form, landing-page, and chat-widget definitions under authenticated `/api/lead-capture-forms`, `/api/lead-landing-pages`, and `/api/lead-chat-widgets`: collection `GET`, `POST`; item `PATCH` | Member read; Admin mutation | Session tenant and tenant-scoped item SQL | None | Private defaults | Request telemetry | `lead_forms_test.go` covers handlers/roles/public IDs; explicit cross-tenant matrix is incomplete |
| `GET /api/lead-capture-submissions` | Admin | Session tenant; the service filters submission, form, contact, reviewer, and run aggregates by the session organization | None; read-only while a managed workspace is suspended | Private defaults; exact allowlisted status/form filters; newest 50-row bound. The response contains submitted business fields and consent context, so members/viewers are denied | Request telemetry plus aggregate unreviewed/legitimate/spam and oldest-unreviewed gauges without tenant, form, contact, or payload labels | Handler admin/member tests; disposable-PostgreSQL filter, retained-payload, cross-tenant, and aggregate-health acceptance; focused UI and Chromium review journey |
| `POST /api/lead-capture-submissions/{submissionID}/review` | Admin | Session tenant plus tenant-scoped submission/contact/run/job locks; foreign IDs are `404`. The service rechecks active membership inside the effect transaction | Managed tenant write policy; restoring the quarantine consumes a contact-capacity reservation atomically, while self-hosted remains unrestricted | Private defaults; bounded JSON/note; 16–200-character idempotency input is retained only as SHA-256 in a tenant/submission-bound, non-portable request ledger so historical delayed retries cannot reapply an older decision. Spam archives the exact captured contact and cancels queued work; recovery matches the exact quarantine timestamp, preserves prior runs/tasks, and creates at most one successor per latest cancelled rule. Generic archive restore is blocked | Request telemetry, activity/audit transition evidence, aggregate review state/age gauges, and stale/unavailable alerts without customer labels | Stable handler errors/role/hosted-policy coverage; disposable-PostgreSQL digest/replay/mutation/cross-tenant/repeated-cycle/run-lineage/archive-guard tests; focused UI and clean-browser quarantine/recovery acceptance |
| `GET /api/public/landing-pages/{slug}`, `GET /api/public/lead-chat-widgets/{publicID}` | Public | Token tenant | None | Shared public abuse budget: 120/client/minute | Request and bounded abuse-decision telemetry | `lead_forms_test.go`, route-family rate-limit tests, and concurrent PostgreSQL acceptance |
| `POST /api/public/lead-capture-forms/{publicID}/challenge` | Public | The opaque public form ID resolves one active form and organization; inactive/foreign IDs are indistinguishable from missing | None; issuing security material has no tenant effect and does not reveal hosted state | Shared public abuse budget: 20/client/minute. The service stores only a SHA-256 token digest, exact consent snapshot, and timestamps; a 256-bit token is usable after two seconds, expires after 30 minutes, and is removed 24 hours after expiry by bounded `SKIP LOCKED` cleanup | Request, abuse-decision, and aggregate `challenge_issued`/`rejected`/`error` telemetry without tenant, form, token, address, or browser labels | Handler and route-budget tests; disposable-PostgreSQL expiry, digest-at-rest, cross-form, and cross-tenant acceptance |
| `POST /api/public/lead-capture-forms/{publicID}/submissions` | Public | Token tenant; the form and challenge jointly control the destination organization. The effect transaction locks and revalidates the active form plus organization/form-bound challenge before writes | The locked destination organization is checked for subscription writability and contact capacity before a new effect; an exact already-consumed replay returns the original tenant/form submission before hosted policy so a lost success response remains recoverable. Inactive/limited new work is returned only as leak-safe `503 FORM_UNAVAILABLE` | Shared public abuse budget: 20/client/minute; bounded JSON/form body; explicit consent; delayed one-time challenge. Exact normalized request digest retries return the original effect; changed, expired, premature, missing, cross-form, and cross-tenant attempts fail closed. Contact, activity, submission with consent text/time, and challenge consumption commit atomically. New rows retain no raw address or user agent; historical values are not silently rewritten | Request, abuse-decision, and aggregate accepted/replayed/rejected/error telemetry without tenant, form, contact, address, or browser labels | Handler stable-error/form-decoding tests; disposable-PostgreSQL hosted block/recovery, consent evidence, no-new-network-data, replay/mutation/expiry/concurrency/cross-tenant acceptance; Chromium hosted-page capture/assignment and WCAG scan |
| Lead audiences: `GET` collection and `POST /preview`; `POST` collection and item `PATCH` | Member read/preview; Admin create/update | Session tenant and tenant-scoped definitions/query | None | Private defaults | Request telemetry | `lead_audiences_test.go`; runtime delivery does not exist |
| Marketing email and nurture campaigns: collection `GET`, `POST`; item `PATCH` | Member read; Admin mutation | Session tenant and tenant-scoped definitions | None | Private defaults | Request telemetry | `marketing_campaigns_test.go`, `nurture_campaigns_test.go`; no delivery worker |
| Lead scoring: rules `GET`, `POST`, `PATCH`; `POST /api/contacts/{contactID}/lead-score` | Member read; Admin rule mutation; Writer evaluation | Session tenant and tenant-scoped rule/contact access | None | Private defaults | Request telemetry | `lead_scoring_test.go`; explicit cross-tenant evaluation case remains incomplete |
| Workflow automations: definitions/runs `GET`; definition `POST`, `PATCH` | Member read; Admin mutation | Session tenant and tenant-scoped definitions/runs; deal-task execution re-resolves matching tenant definitions/deal/owner/task/run/audit rows; lead execution rehydrates same-tenant retained form/submission/contact/assignee evidence | None | Private defaults; the normal UI permits one literal task for deal create/real-stage-change/archive or a lead-form submission with one attribution condition, exact assignee, 0–365 whole-day creation delay, and separate due offset. Lead runs/jobs share an immutable schedule, cannot claim early, revalidate source state, and are idempotent; unsupported legacy shapes are skipped | Request telemetry; transactional definition audit, execution run ledger, task activity, execution audit, due-aware polling, and aggregate due-only stalled alerts | Handler role/error tests, UI executable/legacy-visibility/scheduling tests, disposable-PostgreSQL schedule/trigger/replay/owner/assignee/deactivation/bulk/cross-tenant acceptance, and clean-schema browser journey; broader workflow actions do not execute |
| Report definitions: collection `GET`, `POST`; item `PATCH`; bounded results `GET /api/report-definitions/{definitionID}/results`; `GET /api/report-definitions/{definitionID}/export.csv` | Member read/execute; Writer mutation; Admin export | Session tenant; definition lookup and every allowlisted source query bind the session organization. Mutation and export transactions revalidate and lock the active actor membership; missing and foreign IDs are indistinguishable | None; report reads and exports remain available during hosted suspension | Private defaults; results restrict page/page size to 1–100. Executable tables and explicitly marked `grouped_bar_v1` bars use only typed parameterized filters and static SQL expressions, exclude archived rows, and have a five-second query deadline. Pre-contract bars remain hidden and non-executable. A marked bar requires one category, no row columns, and count/sum/average; the client checks result identity and renders an exact accessible table. CSV requests inspect row 10,001 and return `EXPORT_TOO_LARGE` rather than truncating, neutralize spreadsheet formulas, and return a non-cacheable attachment | Request telemetry records route/status/latency, including bounded `409`/`422`/`504` outcomes. Definition changes and successful downloads create tenant/actor/definition-bound audit events including visualization type and execution contract; result payloads and files contain no SQL or internal error | `custom_reports_test.go`, `execution_postgres_test.go`, `pilot_load_postgres_test.go`, focused frontend tests, migration registration, and the PostgreSQL browser journey cover viewer execution, admin-only export, stable errors, all source types, marked and legacy table/bar contracts, archived/foreign exclusion, pagination, aggregation, formula safety, overflow refusal, accessible exact results, audited download, and cross-tenant `404` |
| `GET /api/reports/sales-activity` | Member, including viewer | Session tenant; deal-event and work-activity predicates require the session organization, while an optional owner must have a current or retained membership in that organization | None | Private defaults; strict UTC dates span at most 366 inclusive days, newest deal events are capped at 50, malformed/foreign owner filters fail closed, and pre-ledger periods are labeled partial | Request telemetry; append-only deal-stage snapshots preserve event-time owner/name/pipeline/stage/outcome meaning across later edits | Handler auth/filter/safe-error tests, disposable-PostgreSQL date/owner/activity-link/snapshot/conversion/cross-tenant acceptance, focused UI tests, and clean-schema browser evidence |
| `GET /api/reports/follow-up`, `GET /api/reports/client-health`, `GET /api/reports/client-activity`, `GET /api/touchpoints/{entityType}/{entityID}` | Member, including viewer | Session tenant; every record, task, source, link, and owner predicate uses the session organization. Private email and meeting touches affect health and period counts only for their mailbox/calendar owner or creator; a foreign record is indistinguishable from missing and foreign collection rows remain absent | None | Private defaults; entity types, health states, and activity states are allowlisted. Stale windows are 7–365 days; activity windows are inclusive UTC dates capped at 366 days and execute under a five-second deadline with stable `REPORT_TIMEOUT`. Report results are capped at 100 (25 by default), malformed/foreign owner filters fail closed, and archived/non-client records and archived tasks are excluded | Request telemetry; responses expose bounded source type/ID, source record, occurrence/creation fallback, exact activity/note/task/day counts, open/overdue/due-soon task counts, health reasons, and exact inference rules; no historical health change or issue signal is fabricated | Handler auth/filter/not-found/timeout/safe-error tests, query-normalization tests, disposable-PostgreSQL source/deduplication/privacy/disabled-owner/company/task-rollup/activity/health/cross-tenant acceptance, 500-client performance/isolation gate, focused UI tests, and clean-schema browser/WCAG/foreign-collection evidence |
| Email sequences: definitions `GET`, `POST`, `PATCH`, `DELETE`; `POST /api/email-sequences/{sequenceID}/approve`, `/pause`; enrollments `GET`, `POST`, `DELETE` | Member read; Writer draft/edit/delete/enroll/cancel and safety pause; Admin exact-revision approval | Session tenant and tenant-scoped definition/enrollment/outcome SQL; foreign definition IDs remain indistinguishable from missing; reply completion additionally requires the same active contact email, enrolling mailbox, post-acceptance time, and exact RFC reply reference or unique provider-thread match; delivery feedback requires the same tenant/mailbox/time boundary, exact opaque outbound message ID, and no recipient disagreement | None | Private defaults; creation and edits are draft-only; active content is immutable until paused; any enrollment history prevents content replacement/deletion; only an active definition with current-revision approval can enroll or reach the provider boundary; pause is idempotent and an already claimed provider attempt may finish; suppression exits immediately; ambiguous/uncorrelated replies and feedback fail closed without changing enrollment state; complaint takes precedence over bounce | Request telemetry plus approval/pause and customer-feedback audit events, classified completion and exact retained reply/feedback evidence; aggregate customer bounce/complaint/unapplied alerts; delivery uses structured job logs and operator recovery; bounded correlation identifiers are excluded from portable exports | Handler role/audit tests, focused UI tests, exact MIME/provider contract and DSN/ARF parser tests, and disposable-PostgreSQL approval/revision/enrollment/claim-race/exact-header/unique-thread/ambiguous-thread/feedback/outcome/tenant acceptance plus `sequencerunner/service_postgres_test.go`; live-provider bounce/complaint/reply evidence remains open |
| Calls: `GET /api/calls`; `POST /start`, `/log`; item `PATCH /complete`, `/recording` | Member read; Writer mutation | Session tenant and tenant-scoped records | None | Private defaults | Request telemetry | `call_logs_handlers_test.go`; fake provider only, with contact controls omitted from production UI/bundle |
| SMS: `GET /api/sms-messages`; `POST /api/contacts/{contactID}/sms`, `/api/sms-messages/log`, `/api/sms/opt-outs` | Member read; Writer mutation | Session tenant and tenant-scoped contact/message/suppression | None | Private defaults | Request telemetry | `sms_handlers_test.go`; fake provider/manual inbound only, with contact controls omitted from production UI/bundle |
| Calendar: events `GET`, `POST`, item cancel `PATCH`; own availability `GET`, `PUT`; booking links `GET`, `POST`, `PATCH` | Member read; Writer mutation | Session tenant; availability also session user; item SQL is tenant-scoped | None | Private defaults | Request telemetry; reminder delivery has structured job logs | `calendar_handlers_test.go`, `calendar/reminders_postgres_test.go`; public booking/provider loop absent and contact/booking controls omitted from production UI/bundle |
| Contacts: collection/item `GET`; `POST`, item `PATCH`, `DELETE`; `POST /{contactID}/email`, `/sms` | Member read; Writer mutation | Session tenant and tenant-scoped record SQL | Only collection `POST` has Plan create: `contacts` | Private defaults | Request telemetry; record email persists message/activity and provider result | `contacts_test.go`, `contact_email_test.go`, `cross_org_test.go`, `core_tenant_isolation_postgres_test.go`, `billing_test.go`, and the executable tenant-isolation matrix |
| `GET`, `POST /api/email-unsubscribe/{token}` | Public HMAC-signed token. `GET` validates and renders a confirmation without changing state; `POST` requires the exact RFC 8058 `List-Unsubscribe=One-Click` form value and is idempotent | Token tenant; the signed organization and normalized recipient are the only write selectors | None; compliance opt-out remains available during hosted suspension | Both methods share the public 20/client/minute budget. Responses are non-cacheable, non-indexable, no-referrer, framed out, and disclose no recipient; POST accepts only URL-encoded or multipart form data and never redirects | Request and bounded abuse-decision telemetry | Handler GET/POST/content/security tests, MIME/provider-contract tests, route-family rate-limit tests, hosted recovery-policy coverage, and disposable-PostgreSQL tenant/idempotency/precedence acceptance |
| `GET /api/email-messages/open/{trackingToken}`, `GET /api/email-messages/click/{clickToken}` | Public random 256-bit token created only for an explicitly acknowledged one-to-one send | Token resolves through its message; no tenant/record selector is accepted from the caller | None | Tracking is off by default and active for 90 days; expired opens no-op and expired clicks redirect without collection. Shared 300/client/minute budget with separate open/click scopes fails closed; redirect targets are stored validated HTTP(S) values; responses are no-store, no-referrer, and non-indexable | Bounded request/abuse decisions plus aggregate cleanup outcome/count/last-run metrics and alerts; no client address, user agent, referrer, tenant, recipient, token, or target label | Handler/privacy-header and route-family limit tests, explicit default-off composer/API tests, disposable-PostgreSQL consent/expiry/redirect/scrub/replay acceptance, migration tests, metrics privacy tests, and promtool alerts |
| Email messages/inbox: `GET /api/email-messages`, item `GET`, own/shared-inbox `GET`; shared-inbox item `PATCH`; thread `GET`, reply `POST`; uncertain-reply resolution `POST` | Member read; Writer coordination/reply, with viewer denial before mutation and at service boundaries. Unscoped private-message view requires Admin. Sharing/unsharing re-resolves an active actor; only the mailbox owner or Admin may change privacy. A shared inbound message permits active writers to reply only through their own connected mailbox. Private replies require mailbox-owner/Admin access. Only the original sender may retry; owners/admins may confirm sent or mark not sent for another sender | Session tenant and own view user. Thread roots, sources, reply intents, outbound records, memberships, and privacy predicates are organization scoped; correlation stays within one mailbox. Foreign messages/replies remain missing. The provider sender is loaded from the actor in the server-side session or the immutable original reply actor, never caller input | None | Inbound private by default; explicit disclosure before sharing and confirmation before privacy recovery. Reply requires a 16–200 character idempotency key, immutable request hash/content/header snapshot, current-sender and suppression checks immediately before effect, one claim, one unresolved request per actor/thread, no automatic retry, stable RFC reply headers, and tracking off. Claims older than five minutes become operator-reviewable uncertain without a provider call; every recovery action requires explicit duplicate-risk confirmation. Portable exports include shared reply content/state but remove hashes and correlation IDs; private intents remain excluded | Bounded request/provider telemetry; content-free privacy/coordination and uncertain-resolution audit events; aggregate send-claim/uncertain/recovery metrics and validated alerts without tenant/message/address labels | Handler role/privacy/idempotency/suppression/thread/uncertainty tests, MIME header/injection tests, focused Mailbox/Team Inbox conversation and recovery UI tests, workspace export schema/privacy coverage, `shared_inbox_postgres_test.go`, and `replies_postgres_test.go` tenant/role/replay/claim/finalize/recovery/correlation acceptance; approved live Gmail/Microsoft/SMTP evidence remains open |
| Companies: collection/item `GET`; `POST`, item `PATCH`, `DELETE`; `POST /{companyID}/email`, `POST /{companyID}/linked-contacts` | Member read; Writer mutation | Session tenant and tenant-scoped record SQL; both linked-person creation and linked-contact replacement accept only active records in the session organization. Missing, archived, or foreign relationship IDs reject the whole transaction with the same `404` as a missing record | Linked-person creation has Plan create: `contacts`; company creation itself is not metered | Private defaults; linked-person creation reserves contact capacity and transactionally commits the normalized non-client contact, typed custom values, primary-safe link, company timestamp, and both activity records, so a failed link leaves no orphan contact. Replacement verifies one inserted link per requested ID, so a partial relationship set cannot commit | Request telemetry plus contact/company activity evidence | `companies_test.go`, `company_people_handlers_test.go`, `contacts/company_people_postgres_test.go`, `core_tenant_isolation_postgres_test.go`, `record_email_test.go`, `cross_org_test.go`, capacity-path PostgreSQL coverage, focused UI stale-response coverage, and the clean browser linked-person/duplicate journey |
| Client review schedules: `GET`, `PUT`, `DELETE /api/client-reviews/{entityType}/{entityID}` | Member read, including viewer; Writer mutation | Session tenant; entity must be an active customer company or individual client, assignee an active same-tenant member, and schedule/task/reminder writes use tenant-scoped composite identities | None | Private defaults; one schedule per client, review/renewal allowlist, 0/1/3/6/12-month cadence, due time bounded from one year past to ten years future, serializable mutation, managed-task and client lifecycle blockers prevent orphaned state | Request telemetry plus transactional client activity/audit, ordinary task activity, versioned notification/reminder job evidence, and dashboard due buckets | Handler auth/role/scope/body/error tests, focused UI tests, disposable-PostgreSQL lifecycle/replay/reminder/dashboard/cross-tenant/bulk/archive acceptance, and clean-schema browser recurrence journey |
| Deal pipelines/stages: collection `GET`, `POST`; pipeline `PATCH`; stage `POST`, `PATCH`; stage order `PUT` | Member read; Admin configuration | Session tenant; pipeline/stage locks, deal-usage checks, and every read/write use organization predicates; foreign pipelines/stages are indistinguishable from missing records | None | At most 10 pipelines and 20 stages per pipeline; 100-character unique names; open probability is 0–100 while won/lost are fixed at 100/0; exact complete stage-order payload; serializable configuration writes; a used stage may be renamed/reweighted but cannot be reclassified between open/won/lost | Request telemetry plus transactional pipeline/stage create, update (including probability before/after), and reorder audit events; the UI explains stable stage identities and protected outcomes | Handler admin/member/conflict/scope tests, disposable-PostgreSQL uniqueness/order/default/usage/probability/disabled-actor/cross-tenant/audit acceptance, UI configuration and forbidden-role tests, and the clean-schema browser configuration/deal-continuity/forecast journey |
| Quote templates: collection `GET`, `POST`; policy `GET`, `PUT`; merge-token catalog `GET`; item `PATCH`, `DELETE` | Member read; Admin mutation | Session tenant; every template and policy query is organization-scoped. Updates and archive require the current revision; a foreign ID is indistinguishable from missing | Reads have no entitlement; create/update/archive/policy are normal hosted writes | Private defaults; names are unique among active tenant templates; terms, validity, delivery defaults, and the finite merge-token vocabulary are bounded. Revision compare-and-swap rejects stale edits. Enabling workspace- or template-required approval transactionally requires a different active owner/admin, and archive preserves historical quote references | Request telemetry plus transactional create/update/archive/policy audit evidence; mutable definitions never rewrite a finalized quote | Handler member/admin/body/conflict/error tests, module validation tests, freshly migrated PostgreSQL revision/conflict/cross-tenant/approver/immutable-snapshot acceptance, focused admin UI tests, workspace-portability coverage, and browser journey |
| `GET /api/deal-quote-approvals`; `POST /api/deals/{dealID}/quotes/{quoteID}/approval` | Admin | Session tenant plus tenant/deal/quote-bound approval and immutable PDF digest; the deciding active owner/admin must differ from both requester and quote creator | Read has no entitlement; decision is a normal hosted write | Pending-only list is capped at 100 oldest-first. Decision requires a 16–200 character digest-only idempotency key; rejection requires a bounded note. The approval row and exact quote PDF are locked together; exact replay by the same approver returns retained evidence, while changed or terminal reuse conflicts. Self-approval, stale digest/state, disabled actors, and cross-tenant IDs fail closed | Request telemetry; transactional decision activity, audit, requester notification, finite pending/approved/rejected gauges, and oldest-pending-age alert | Handler unauthenticated/role/error/idempotency tests, freshly migrated PostgreSQL self/creator/disabled/cross-tenant/replay/rejection/queue/notification/audit/metrics acceptance, focused hook/deal/admin UI tests, portable-evidence redaction, and browser journey |
| Deals: collection/item/current-data quote and finalized-quote PDF `GET`; `POST`, item `PATCH`, `DELETE`, email/stage/quote-finalization/expired-quote-reissue `POST`/`PATCH`, line-items `PUT`; signature request void/conversion `POST` and certificate `GET` | Member read; Writer mutation, including reissue, void, and signed-quote conversion; certificate read follows ordinary deal membership | Session tenant and tenant-scoped record/child SQL; finalized quote, template revision, approval digest, reissue lineage, FX rate, signature request, conversion stage, conversion activity, and certificate lookups require the session organization, deal, and child/currency identities together; owners must be active same-tenant members; matching task rules, assignment notifications, and won-deal handoff execute within the same organization transaction | Only collection `POST` has Plan create: `deals`; a security-reducing void remains available during hosted read-only mode, while creating/reissuing/sending/converting uses normal hosted writability | Private defaults; quote finalization, expired-quote reissue, and signed conversion require 16–200 character digest-only idempotency keys. Finalization with a template accepts only its exact active revision and exact retained terms, snapshots rendered delivery defaults, and requires a different active owner/admin when the request, template, or workspace policy requires approval. Finalization/reissue locks the workspace base currency and either an identity rate or newest tenant/currency rate effective on the document date; a missing/future/foreign rate fails before evidence exists. Reissue also locks the open deal, expired source quote, native signature evidence, template reference, and approval state; it rejects active quotes, signed evidence, unresolved sends, repeated replacement, and cross-tenant lineage, then creates one new PDF/hash/version/FX snapshot and fresh required approval while retaining source evidence and voiding an expired pending signature in the same transaction. Conversion locks the native signed request and open deal together, accepts only a same-tenant won stage and valid close review, then binds the retained certificate to exactly one stage activity, stage-event snapshot, actor, outcome, automation execution, and client handoff. Exact replay returns one effect; changed reuse conflicts. Staff cannot create a detached signature record or mark one signed/declined. Historical manual rows are read-only and explicitly not signature evidence. Expected-close filters require a valid non-reversed date range; won creates/transitions and edits require a live account relationship | Request telemetry; finalization, approval, reissue, native terminal responses, and conversion retain immutable PDF/certificate SHA-256, exact template revision, rendered delivery defaults, decision evidence, source/replacement lineage, FX rate/source/reporting total, stage/close snapshots, activity, and redacted audit evidence. Email provider results, assignment notifications, automatic task/run/audit evidence, and won `client.handoff` evidence are also persisted | Handler role/state/idempotency/template/approval tests; `core_tenant_isolation_postgres_test.go`; disposable-PostgreSQL FX effective-date/retry/tenant/history, exact-template/approval/reissue, reissue replay/concurrency/tenant/evidence/signature recovery, and signature ceremony/conversion replay/concurrency/tenant/certificate/void/handoff tests; `assignment_notifications_postgres_test.go`, `record_email_test.go`, `cross_org_test.go`, billing/export/forecast/task-rule/win/handoff/report tests; the executable tenant-isolation matrix; focused commercial UI tests; and clean browser journey |
| `POST /api/deals/{dealID}/quotes/{quoteID}/deliveries`; `POST /api/deal-quote-deliveries/{deliveryID}/resolve` | Writer; the original sender owns retry, while owner/admin may confirm or reject another sender's uncertain outcome | Session tenant plus immutable tenant/deal/quote or tenant/delivery lookup; active role and stored actor are server-resolved. Sender identity, recipient suppression, and exact approval state are rechecked immediately before the provider effect | Normal hosted writability; no separate feature/plan grant | Private defaults; 16–200 character idempotency key is actor-scoped and digest-only; exact request reuse replays one intent and conflicting reuse is rejected. Every preparation and claim rejects an expired quote. For a review-required quote, both boundaries require retained `approved` evidence for the exact PDF digest, so pending, rejected, missing, or stale evidence cannot be bypassed between intent and provider claim. Optional signature delivery transactionally creates one quote/recipient-bound native request and links it to the same delivery. Provider acceptance activates signing, definite failure/confirmed-not-sent voids it, and an uncertain send cannot be bypassed. One unresolved delivery and one active native request per quote; intent and stable RFC Message-ID commit before SMTP/Gmail/Microsoft. Claims older than five minutes become uncertain and never retry automatically | Request/provider telemetry; status/error and provider acceptance correlate to shared outbound email, activity, and audit without storing the secure URL in the CRM body. Aggregate delivery recovery, quote approval state/age, and awaiting/expired/signed/declined/voided/awaiting-conversion/converted signature gauges and alerts have no tenant/address/token labels | Handler role/idempotency/approval/provider-error tests; disposable-PostgreSQL approval-after-preparation, replay/race/suppression/recovery/member-lifecycle/signature-state/cross-tenant/expiration acceptance; focused UI retry-key/approval/evidence/resolution tests; real-SMTP PostgreSQL browser journey |
| `GET /api/public/quotes/{token}`, PDF and signature-certificate `GET`; receipt, signature, and decline `POST` | Public bearer-token customer flow | A digest of the HMAC-derived 256-bit token resolves exactly one sent delivery, tenant-bound immutable quote, and optional quote/deal-bound native request; no caller-supplied tenant/deal/quote/request ID is trusted | None; customer receipt and terminal signature/decline evidence remain authoritative during hosted suspension | Shared PostgreSQL abuse budget: preview/PDF/certificate 120/client/minute, receipt/sign/decline 20/client/minute. Token is bounded/digest-only, expires after quote validity plus 30 days with a 24-hour minimum; signing additionally ends after the quote-validity day. Invalid is non-disclosing `404`, expired is `410`; responses are private/no-store and no-referrer. Signing requires the exact normalized recipient name, explicit retained consent, and a 16–200 character digest-only idempotency key; exact replay returns one effect, changed reuse conflicts, and terminal state rejects a new key. Decline uses the same replay contract. Neither flow stores an IP address nor browser fingerprint | Request/abuse-decision telemetry; approximate access/download, explicit receipt, signed/declined activity/audit, retained certificate/PDF digests, and aggregate finite-state gauges; no token, recipient, quote, or tenant metric labels | Public handler/rate-limit/UI tests; disposable-PostgreSQL invalid/expiry/replay/concurrency/name/consent/certificate/cross-tenant acceptance; portable-evidence privacy test; and isolated real-SMTP customer sign/WCAG browser journey |
| Notes: `GET`, `POST` | Member read; Writer create | Session tenant and tenant-scoped entity resolution; `@email` mentions resolve only active same-organization members | None | Private defaults | Request telemetry; note, activity, mention, following, and notifications commit transactionally | Handler tests, mention parser test, `core_tenant_isolation_postgres_test.go`, and disposable-PostgreSQL collaboration acceptance |
| Record followers: `GET /api/record-followers`; own `PUT`, `DELETE /api/record-followers/me` | Member, including viewer for own follow preference | Session tenant and user; polymorphic record existence is revalidated with organization predicates before serializable writes | None | Private defaults; follow/unfollow are idempotent | Request telemetry; UI exposes explicit load/follow/unfollow state | Handler organization/role tests and disposable-PostgreSQL cross-tenant, disabled-member, and idempotency acceptance |
| `GET /api/collaboration/activity-digest` | Member | Session tenant; followed scope joins the session user's organization-scoped subscriptions; optional actor remains inside the tenant predicate | None | Private defaults; windows are restricted to 1, 7, or 30 days and results to 50 | Request telemetry | Handler filter tests, PostgreSQL followed/team/actor/cross-tenant acceptance, and UI filter/navigation tests |
| Imports: `POST /api/imports/preview`, collection `GET`, `POST`; `GET /api/imports/{batchID}/errors.csv`; `POST /api/imports/{batchID}/rollback`; four `GET /api/export/{contacts,companies,deals,tasks}` | Admin | Session tenant and actor flow into mapped preview/write/history/error/rollback queries; active custom definitions and batch/imported-row identities are organization and record-type scoped | None | 2 MiB/1,000-row import ceiling; explicit core/custom mapping with typed/required validation; stable per-upload idempotency key and source hash; serialized imports; changed-record-safe rollback; synchronous exports request up to 10,001 rows and refuse overflow with `EXPORT_TOO_LARGE`, never a silently partial file | Request telemetry plus completion/rollback audit events, durable progress/error rows, operator UI, stated export boundary | Handler role/scope/overflow tests, disposable-PostgreSQL mapping/idempotency/custom-field/cross-tenant/error/rollback acceptance, 1,000-row import and exact/overflow 10,000-row export load gates, UI tests, and combined browser import/rollback/all-export journey; durable full-workspace portability is covered by the separate workspace-export surface |
| Bulk data operations: `POST`, `GET /api/data-operations/bulk`; `POST /api/data-operations/bulk/{operationID}/rollback` | Member read; Writer execute/rollback | Session tenant; every selected id must resolve to one active record in that tenant or the whole write is rejected; target assignees, deal-assignment notifications, and deal-archive task rules remain in the same tenant and transaction; operation/row identities use composite tenant keys | None | 100-record ceiling; normalized request digest and stable idempotency key; destructive archive and rollback confirmations; serializable transaction; deal owner generations plus per-operation/per-deal automation keys prevent duplicate assignment events and archive tasks; rollback skips records changed after the operation | Request telemetry, persistent in-list history/recovery UI, per-record activities, preference-aware deal-assignment notifications, aggregate completion/rollback audit events, and task-rule run/audit evidence | Handler role/scope/conflict tests, disposable-PostgreSQL idempotency/foreign-tenant/foreign-assignee/change-aware rollback, deal-assignment, and task-rule replay acceptance, component retry/recovery tests, and browser status/undo journey |
| Archive recovery: `GET /api/data-operations/archive`; `POST /api/data-operations/archive/{entityType}/{entityID}/restore` | Member read; Writer restore | Session tenant; the four record tables, owner label, dependency checks, and duplicate-merge history use organization predicates; foreign records are indistinguishable from missing records | None | 100-record maximum (UI requests 50); four allowlisted entity types; restore is serializable; archived/missing records return `404`; archived deal/task dependencies and permanent duplicate-merge sources return `409`; explicit browser confirmation | Request telemetry, per-record `*.restored` activity, transactional `record.restored` audit event, role-aware recovery UI | Handler role/scope/status tests, disposable-PostgreSQL all-entity/cross-tenant/inactive-actor/dependency/merge-history/audit acceptance, UI member/viewer tests, and clean-schema browser archive/restore journey |
| `GET /api/data-quality/summary` | Member, including viewer | Session tenant; organization profile and every contact/client/deal/task issue query are scoped by `organization_id`, exclude archived records, and join child records through matching tenant keys | None | Read-only; stale window is restricted to 7–365 days; five fixed core queries plus one allowlisted business-profile query; exact counts with at most 25 returned records per queue | Request telemetry; generated time, rule criteria, per-record reason, current count, and direct record links are returned to the live Reports UI | Handler membership/threshold/safe-error tests, disposable-PostgreSQL count/business-profile/archive/cross-tenant acceptance, component threshold/action-link tests, and clean-schema browser report-to-deal journey |
| Duplicate review and merge: `GET /api/data-operations/duplicates`; `POST /api/data-operations/duplicates/merge` | Admin | Session tenant; candidate and history reads are organization-scoped, both active records are locked under the session organization, and foreign/missing records reject the whole transaction | None | 50-pair review ceiling; explicit permanent-merge confirmation in UI; exact source/target version check; normalized field allowlist; serialized idempotent merge; source remains archived; collision-safe relationship consolidation | Request telemetry, persistent merge history, surviving-record activity, aggregate audit event with relationship counts but no copied record payload | Handler admin/scope/conflict tests and disposable-PostgreSQL candidate/idempotency/disabled-actor/cross-tenant/stale-record/relationship-preservation acceptance |
| Custom fields: collection `GET`, `POST`; item `PATCH`, `DELETE` | Member read; Admin mutation | Session tenant; stable definition keys and every definition/value/filter/import/export/duplicate query are organization- and record-type-scoped | None | 25 active fields per record type; five bounded types; 16 KiB value ceiling; field key/type immutable; removing in-use select options is rejected; required fields apply on create/edit/import; archive preserves historical values and requires browser confirmation | Request telemetry plus transactional definition create/update/archive audit events | Handler role/scope/error tests, malformed-number/option/value validation, UI permission and record-flow tests, disposable-PostgreSQL definition/value/filter/import/export/merge/archive/tenant acceptance, and the clean-schema Chromium pilot journey |
| Saved views: collection `GET`, `POST`; item `PATCH`, `DELETE` | Member read; Writer mutation | Session tenant and session user/tenant-scoped item SQL | None | Private defaults | Request telemetry | `saved_views_test.go`, `cross_org_test.go`, and `core_tenant_isolation_postgres_test.go` |
| Tasks: collection/item `GET`; `POST`, item `PATCH`, `DELETE` | Member read; Writer mutation | Session tenant and tenant-scoped record SQL; assignees must be active same-tenant members; automatic deal tasks resolve an active owner or triggering-actor fallback | None | Private defaults; exact due views are allowlisted; task and versioned reminder changes commit together; assignment events use per-recipient/version keys; automatic creation is bounded to one task per matching rule/event and protected by a unique run key; managed client-review tasks reconcile due/assignee/completion in the same transaction, reject direct archive/bulk mutation, and advance recurring cadence exactly once | Request telemetry; task/reminder activity, notification, durable job, and client schedule activity/audit evidence; stale effects and replayed recurring completion become observable no-ops | `tasks_test.go`, `cross_org_test.go`, `role_permissions_test.go`, `core_tenant_isolation_postgres_test.go`, task-reminder, client-review, and deal-task-rule PostgreSQL acceptance, user-lifecycle reminder acceptance, focused UI tests, and browser due/recurrence regression |
| Dashboard: `GET /summary`; `PUT /sales-quotas/{userID}` | Member read; Admin quota mutation | Session tenant plus tenant-scoped active members, stages, deals, rates, client schedules/tasks, and target user; unassigned is an explicit tenant-local bucket | None | Private defaults; requested forecast ranges require both dates, ordered, at most one year; missing-close open deals are included and missing-close wins use update date; missing FX is disclosed; client obligations include only active task-backed customer schedules and return at most six records | Request telemetry plus audited pipeline probability changes and exact overdue/due-within-30-days/later client-obligation counts | Handler invalid-period/role tests, disposable-PostgreSQL probability/period/unassigned/FX/client-review/tenant acceptance, focused UI tests, and clean-schema weighted-forecast/renewal browser evidence |
| Organization profile: `GET`; profile `PATCH`; exchange-rate `PUT` | Member read; Admin mutation | Session tenant | None | Private defaults | Request telemetry plus audit events for mutations | `business_profile_test.go`, `role_permissions_test.go` |
| Notifications: list/count `GET`; item read `PATCH`; read-all `POST`; internal retention and aggregate health | Member, including viewer acknowledgement writes; no public/admin cleanup route | Session tenant and session user for API reads/writes; cleanup is global internal lifecycle work; protected metrics contain no tenant/user/record labels | None | Private defaults; per-user task/deal assignment and task-reminder preferences; acknowledged rows retained 90 days, unread rows 365 days; independent 500-row `SKIP LOCKED` cleanup classes | Request telemetry; mention/follower/task assignment/deal assignment/task reminder events have per-user idempotency keys, deal changes fail atomically if their notification cannot be written, and task reminders add activity evidence; aggregate backlog age, finite reviewed event mix, recipient concentration, retention outcomes, and deletion counts | Notification handler/module tests; disposable-PostgreSQL retention boundary/idempotency/concurrent-lock/event-bucketing acceptance; PostgreSQL collaboration/task-reminder/direct-bulk-lifecycle deal-assignment acceptance; metrics privacy tests; promtool alerts; profile preference handler test; UI event filters/deep-link tests; and clean browser recipient journey |

## Background operations

| Operation | Trigger / authority | Tenant boundary | Entitlement / pacing | Idempotency and recovery | Observability | Test evidence |
| --- | --- | --- | --- | --- | --- | --- |
| PostgreSQL job worker | Internal process loop; no public trigger | Every claimed job has `organization_id`; handlers repeat tenant predicates/locks | Every tenant effect handler except billing reconciliation and portability export checks hosted writability; bounded batch, poll interval, lease, handler timeout; hourly retention compacts successful detail after 30 days and removes successful rows after 400 days in 500-row `SKIP LOCKED` batches | Stable enqueue key, `SKIP LOCKED` lease, claim token, capped execution failures/dead letter/admin replay; expected policy blocks release to `retryable` after 15 minutes and undo only the current claim's attempt; active and dead jobs are excluded from retention, only seven reviewed source-guarded types are eligible, and source ledgers remain the duplicate guard beyond the successful-row window | Structured logs, bounded `deferred`/retry/dead outcomes, retention counts/errors, aggregate durable queue counts/lag, tenant queue health/admin UI | `jobs/service_test.go`, disposable-PostgreSQL lifecycle/attempt-preservation/retention boundaries and locked-candidate skip, billing guard tests, handler recovery tests |
| Workspace portability generation and expiry | Admin request transaction enqueues one job; an internal hourly idempotent cleanup pass clears expired artifact bytes | Job and export record carry the same organization; every dataset query is tenant-scoped or joins through a tenant-scoped parent; a runtime guard refuses generation when a new `organization_id` table lacks an explicit exported/excluded classification | Explicit hosted offboarding recovery path; one active generation and three retained ready artifacts per workspace; repeatable-read snapshot, three execution attempts, 3-minute query/4-minute worker limit, 200 MiB uncompressed and 50 MiB compressed ceilings, seven-day artifact TTL | Request/job keys are stable; an already-ready unexpired artifact is a safe no-op; failures retry/dead-letter; cleanup retains metadata/audit while clearing bytes | Durable status/error and labeled queue/replay UI, structured cleanup success/failure logs, request/ready/download audits, SHA-256/record-count manifest | `workspaceexports/service_postgres_test.go`, handler, CORS, billing/Operations UI, and browser tests |
| Mailbox job discovery | Internal scheduler scans persisted `next_sync_at` | Account organization and user are loaded together; enqueue carries organization | Delivery is deferred in hosted read-only mode; no hosted message/account quota yet; one-minute default discovery cycle and bounded scan | Stable account/cycle key; provider message ID dedupe; success advances persisted next run | Structured scheduler/job/provider errors; queue/recovery UI | `mailboxsync/jobs_postgres_test.go`, billing guard, service tests |
| Email engagement retention | Internal API-start and hourly scheduler; no public/admin cleanup route | Global lifecycle scan selects only expired opted-in/legacy tracked message IDs; protected metrics contain no tenant, message, recipient, or token labels | 500-message maximum per pass; `FOR UPDATE SKIP LOCKED` permits multiple API instances | Message and link observations are scrubbed transactionally; click mapping remains usable; purge timestamp makes replay a no-op | Structured failure/completion logs plus bounded run, purged-count, timestamp, and success metrics with stale/error alerts | Fresh-schema PostgreSQL default-off/explicit-window/expiry/redirect/scrub/replay acceptance, migration registration, metrics tests, and promtool alerts |
| Connected-mailbox reply recovery | Internal API-start and minutely scheduler; no public/admin bulk-recovery route. Individual uncertain outcomes are resolved only through the authenticated thread UI/API policy above | Global lifecycle scan selects only durable `sending` reply IDs older than five minutes; thread/source/outbound rows and interactive resolution remain tenant scoped. Protected metrics contain no tenant, actor, mailbox, message, or recipient labels | 100-request default/1,000 maximum per pass; `FOR UPDATE SKIP LOCKED` permits multiple API instances; update rechecks `sending` state | Stale claims become `uncertain` exactly once without a provider call. No automatic retry; per-actor/thread partial uniqueness prevents a new request key from bypassing resolution | Structured aggregate warning/failure logs plus bounded outcome/recovered/timestamp/success counters and current sending/stale/uncertain gauges with unavailable/error/stale/unresolved alerts | Scheduler/unit and metrics tests, promtool rules, handler/UI explicit-resolution tests, migration/index guard, SMTP definite/uncertain contract tests, and disposable-PostgreSQL recovery/replay/role/tenant acceptance |
| Finalized-quote delivery recovery | Internal API-start and minutely scheduler; individual uncertain outcomes are resolved only through the authenticated quote UI/API policy above | Global lifecycle scan selects only durable `sending` quote-delivery IDs older than five minutes; delivery/quote/email rows and interactive resolution remain tenant scoped. Protected metrics contain no tenant, actor, quote, sender, recipient, message, or token labels | 100-delivery default/1,000 maximum per pass; `FOR UPDATE SKIP LOCKED` permits multiple API instances; update rechecks `sending` state | Stale claims become `uncertain` exactly once without a provider call. No automatic retry; one-unresolved-delivery and actor-scoped idempotency constraints prevent a new key from bypassing resolution | Structured aggregate recovery logs plus bounded outcome/recovered/timestamp/success counters and current sending/stale/uncertain gauges with unavailable/error/stale/unresolved alerts | Scheduler/unit and metrics tests, promtool rules, handler/UI explicit-resolution tests, migration/index guard, SMTP provider-boundary browser acceptance, and disposable-PostgreSQL recovery/replay/role/tenant/expiry acceptance |
| Calendar reminder delivery | Transactional enqueue when an event is scheduled | Job organization must match locked event/reminder organization | Delivery is deferred in hosted read-only mode; no plan-specific quota; due time comes from reminder record | Stable reminder key; row lock; duplicate/cancelled reminders are no-ops; admin replay | Structured job logs, queue UI, notification/activity evidence | `calendar/reminders_postgres_test.go`, billing guard tests |
| Task reminder delivery | Transactional enqueue from direct task writes, deal automation, bulk/rollback, restore, and user lifecycle paths | Job organization must match the locked reminder and task; current assignee must remain an active member of that organization | Delivery is deferred in hosted read-only mode; due-soon is a rolling 24-hour window; per-user in-app preference is rechecked at delivery | Stable ledger/job and notification keys; task generation and row locks; stale, replayed, completed, archived, reassigned, or opted-out reminders are no-ops; admin replay | Structured job logs, queue UI, reminder ledger status, notification/activity evidence | `taskreminders/service_postgres_test.go`, billing guard, user-lifecycle PostgreSQL acceptance, handler/UI/browser due-query and preference evidence |
| Email sequence delivery | Transactional enqueue on enrollment/step advance | Job organization must match enrollment, sequence, contact, and delivery rows; the worker rechecks active exact-revision approval immediately before claiming the delivery; later DSN/ARF application rechecks tenant, mailbox, exact RFC message, acceptance time, and recipient | Provider effects are deferred in hosted read-only mode; paused/unapproved definitions defer without consuming job attempts; Stripe-hosted runtimes atomically enforce configurable shared fixed-window defaults of 1,000 eligible attempts/tenant/24h and 100/sender/1h before claim; exhaustion defers without an attempt; fake/self-hosted billing has no hosted cap; sequence schedule controls due time; suppression is a terminal enrollment exit | Delivery snapshot/ledger plus an opaque RFC `Message-ID` persisted before claim; accepted Gmail message/thread receipts commit with enrollment advancement; suppression check; atomic approval check at the provider-effect boundary; tenant/sender budget keys are hashed; grouped budget denial rolls back every reservation; recovered `sending` and ambiguous provider outcomes become `uncertain`; exact feedback is idempotent and complaint-dominant; explicit audited confirm/retry preserves the prepared message identifier | Structured job logs, bounded worker `deferred` outcome, persisted job deferral reason and delivery/correlation/feedback state, tenant-scoped accepted/bounced/complaint counts, aggregate feedback alerts, queue/recovery UI; provider identifiers never become metric labels | `ratelimits/service_postgres_test.go`, `emailsequences/approvals_postgres_test.go`, `emailsequences/outcomes_postgres_test.go`, `emailmessages/delivery_feedback_postgres_test.go`, `sequencerunner/service_postgres_test.go`, exact SMTP/Gmail/Microsoft MIME contracts, billing guard, background job handler tests |
| Stripe billing reconciliation | Internal 15-minute discovery loop selects provider state not verified within six hours | Job, stored organization/customer/subscription references, subscription metadata, and every invoice must identify the same tenant | Hosted Stripe only; bounded 50-tenant discovery batch and recent 25-invoice provider window | Stable subscription/last-success generation key, leased retry/dead-letter queue, serializable tenant lock, webhook ordering cursor, and admin replay | Provider outcome metrics, tenant attempt/success/error evidence, audit event, structured job logs, entitlement freshness, queue/recovery UI | Provider HTTP-contract tests plus disposable-PostgreSQL schedule/dedupe/order/invoice/retry/cross-tenant acceptance and focused Operations UI tests |
| Billing usage snapshot | Internal 15-minute discovery loop selects workspaces without an observation in the current UTC day | Job organization is the only tenant input; the handler reruns the same organization-scoped repeatable-read source reconciliation as the member API | All database-backed runtimes; explicitly exempt from hosted suspension because it is billing-control evidence. Bounded 100-tenant discovery batch | Stable tenant/UTC-date key, active-job suppression, leased retry/dead letter/admin replay, bounded serialization retries, update-in-place period evidence | Structured scheduler/worker logs, queue metrics and labeled Operations recovery, retained observation time/basis/sources | Disposable-PostgreSQL daily schedule/dedupe/concurrency/worker/current-evidence tests and focused Operations UI test |

Database migrations execute as an explicit deploy/startup command, not as an unbounded background loop. Provider calls made directly from authenticated request handlers are covered by their HTTP rows and remain candidates for future transactional-outbox migration.

## Findings that constrain maturity claims

- Authentication and role helpers are consistent. The 25-row executable PostgreSQL matrix now covers every promoted service family, while route-specific handler tests cover non-disclosing and forbidden-role behavior. Hidden Part II foundations remain ineligible for promotion until they add their own matrix row and proportional negative paths.
- Hosted lifecycle enforcement is centralized across the current private route set, public lead writes, non-billing tenant workers, and persistent role-aware frontend controls. A credential-free Stripe HTTP/PostgreSQL contract journey now covers the app boundary, and a retained source-reconciled usage view covers seats, active records, sent email, successful automation/jobs, and estimated tenant row bytes. The hosted catalog exposes enforced capacity but deliberately grants no unapproved features; the UI does not present incomplete API/SSO/automation/reporting outcomes or hard-coded price hints as Stripe promises. Existing seat/contact/deal capacities are concurrency-safe: a short-lived tenant/resource claim is counted with committed usage, and the effect transaction holds the tenant lock while deleting the claim, so another request observes either side of the atomic handoff. All direct, import, public lead, reactivation, and archive-recovery paths use that contract; the internal claim ledger is excluded from exports and storage billing evidence. Hosted SaaS remains a foundation because feature policy and broader period-meter quotas are not approved/enforced, API usage has no external API surface to measure, dunning/proration/resubscription and retention policy remain undecided, and credentialed Stripe test-mode acceptance has not been approved/run.
- Private endpoints have no per-tenant/session request budget. Public limits coordinate through PostgreSQL across processes and restarts. Lead writes additionally require a delayed digest-only one-time challenge and explicit consent, but these remain application controls rather than a volumetric DDoS or IP/domain reputation boundary. Add an approved production edge/WAF before promoting public lead generation beyond foundation maturity.
- Global logs provide request correlation; protected time-series request/database/provider/job/notification/email-engagement-retention/backup metrics and reference alert rules now exist. Production scrape credentials, an alert destination, redaction verification, SLO validation, and telemetry-store retention policies remain operator/pilot work.
- Sensitive writes are not uniformly represented in audit events. Each converged feature slice must add audit coverage before its maturity is promoted.
- The route guard detects any route-set change, but it does not prove service SQL isolation. Database integration and explicit cross-tenant/forbidden-path tests remain required for each promoted capability.

## Maintenance rule

When adding, removing, or renaming a route or background operation:

1. Review all seven dimensions in this inventory before changing the expected count/digest.
2. Add proportional unauthenticated, forbidden-role, cross-tenant, entitlement, rate-limit, and failure-path tests.
3. Update the capability matrix and runbook if the route changes an exposed outcome or operator workflow.
4. Recompute the sorted registration digest only after review; never update the guard solely to make the test pass.
