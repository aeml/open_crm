# List Endpoint and Pagination Inventory

Audit date: 2026-07-23

Registered GET route count: `109`

Registered GET route digest: `cde58e30287477e37d17cd41a2ccd8c42ba33278be7349398460f73e0f87b96e`

This is the Phase 1 inventory for every production GET route that can return a
collection or a large generated result. The executable guard derives the count
and digest from every production `http.ServeMux` registration, including GET
routes that return one record. Any GET route addition, removal, or rename must
therefore reopen this review instead of silently introducing an unbounded list.

The security and tenant-isolation inventories remain authoritative for
authentication, role, and SQL ownership. This document records result
cardinality, ordering, overflow behavior, and the evidence-based pagination
decision.

## Contracts

| Surface | Result contract | Stable order / continuation | Overflow and failure behavior | Current evidence and decision |
| --- | --- | --- | --- | --- |
| Core contacts, companies, deals, and tasks: `GET /api/contacts`, `/companies`, `/deals`, `/tasks` | Offset page; default `page=1`, `pageSize=20`; maximum page size 100; maximum offset 50,000; exact filtered totals | Contacts: last name, first name, ID; companies: name, ID; deals: pipeline position, stage position, ID; tasks: completion bucket, due time, ID. Every order ends in the immutable ID tie-breaker | Missing values use defaults. Explicit malformed/non-positive values, page sizes above 100, arithmetic overflow, and offsets above 50,000 return `400` before the service/database. Services repeat the same check for non-HTTP callers | Handler tests cover all four rejection paths and the exact 50,000 boundary. The real-PostgreSQL pilot gate checks 100-row adjacent pages, repeat stability, no overlap, exact totals, tenant separation, and direct-service rejection. Offset paging remains appropriate for the approved hosted ceiling of 50,000 contacts/deals and the UI's page-number navigation; introduce keyset paging only if a larger approved workload or measured plan regression requires it. |
| Company linked people: bounded detail embed plus `GET /api/companies/{companyID}/linked-contacts` | Company detail embeds the first 50 active links. The dedicated member endpoint defaults to 50, caps page size at 100 and offset at 50,000, returns an exact search total, and supports name, email, and relationship-title search | Primary first, then last name, first name, and immutable contact ID. The UI searches explicitly and offers “Load more”; the initial-contact and existing-contact selectors independently use bounded core-contact search pages | Malformed/non-positive/oversized/overflowing pages return `400` before service work and direct callers repeat the bound. Missing, archived, and foreign companies are the same `404`. Generic company `PATCH` ignores the legacy relationship array so a cached incomplete selector cannot replace unseen links; writer-only item `PUT`/`DELETE` own relationship changes. For an individual client, `PUT` atomically replaces the sole person and `DELETE` refuses a zero-link state | Handler and viewer-role tests cover transport and gates. Fresh PostgreSQL acceptance proves 50/50/final pages, exact totals, adjacent-page separation, three-field search, detail bounding, foreign denial, safe ordinary edits, idempotent linking, one-primary repair/promotion, active and archived individual-person replacement, unlink-to-zero rejection, and a 100-row page from 1,000 links under two seconds. The Chromium pilot journey searches and links an existing contact, filters linked people, changes primary, unlinks, and scans the populated control for WCAG A/AA issues. Offset paging matches the core contract and remains measured below budget. |
| Saved report results: `GET /api/report-definitions/{definitionID}/results` | Offset page; default size 50; page and page size each max 100; at most 10,000 addressable rows | Static typed registry supplies deterministic source ordering with ID tie-breaker; response includes `hasMore` | Invalid page is `400`; query deadline is five seconds; unsupported/historical definitions fail closed | Handler/UI/real-PostgreSQL all-source, overflow, timeout, tenant, and 100-row performance acceptance. |
| Shared report dashboard: `GET /api/report-dashboard`, `/api/report-dashboard/results` | The configuration is one tenant singleton with at most six distinct widgets. Results contain the same six or fewer widgets with at most 12 groups each and an explicit `hasMore`; the full saved report remains the drill-down | Widget positions are unique and contiguous. All widget results share one database snapshot and generated timestamp; each grouped result preserves the saved report's deterministic category order | Configuration requires active `grouped_bar_v1` definitions and exact revisions. Results have no caller-controlled page or limit and execute in one read-only repeatable-read transaction under the five-second report timeout; timeout is a stable `504` and the next request can recover | Handler and strict frontend contract tests plus fresh PostgreSQL acceptance cover exact bounds, no-op/stale/concurrent writes, tenant/role denial, 12-group truncation, single-snapshot execution, timeout recovery, and six widgets over 10,000 contacts below two seconds. Chromium covers configuration, exact chart/table rendering, foreign-workspace absence, and WCAG A/AA. |
| Scheduled saved-report overview: `GET /api/report-schedules` | At most 20 retained schedules per workspace, one per saved report, each with 1–10 recipients; the same response includes the latest 20 delivery runs and at most 10 recipient outcomes per run | Schedules order active first, then update time and immutable ID descending. Runs order creation time and ID descending; recipients order normalized email then immutable delivery ID | No caller-controlled page or limit exists because advisory-locked creation enforces the complete 20-schedule workspace catalog and each occurrence captures at most 10 recipients. Pausing preserves the schedule/evidence and does not free a new catalog slot. The service fixes delivery history at 20. Only active owner/admin actors may read it; foreign actors and IDs disclose nothing | Handler and strict frontend response tests plus fresh PostgreSQL role/tenant/history acceptance. The same acceptance serializes two final-slot writers to exactly one 20th schedule, rejects an 11-recipient request, and budgets the full bounded overview below two seconds. Chromium covers configuration, one due fake-provider occurrence, recovery, and foreign-workspace `404`. |
| Operational reports: sales activity, pipeline funnel, follow-up, client activity, client health, data quality, dashboard, collaboration digest | Complete bounded aggregate or a fixed/request-bounded sample (25, 50, or 100 rows depending on the report) | Every record sample has a documented timestamp/ID or semantic grouping order | Date/range/limit validation is explicit where caller-controlled; report queries have bounded deadlines where their runtime can scale | Handler and real-PostgreSQL report reconciliation/performance tests. These are reports, not record browsers; paging is deferred unless a pilot needs complete drill-down rather than the current focused sample/export. |
| Lead submission review: `GET /api/lead-capture-submissions` | First page defaults to 50, explicit limits cap at 100, and exact status counts remain scoped to the selected form | Opaque keyset cursor over immutable creation time then ID, both descending. A later arrival stays above an older continuation and appears on refresh; a review-state mutation intentionally changes membership in a filtered live queue and also reconciles on refresh | Malformed cursors, malformed/non-positive limits, limits above 100, and invalid form/status filters are `400` before service work. Direct callers repeat the limit, form, and status bounds. The admin UI serializes review/refresh/continuation work, appends with ID deduplication, and discards older pages after a mutation | Handler and frontend tests cover parsing, bounds, continuation, mutation refresh, and deduplication. Fresh PostgreSQL acceptance traverses 350 filtered rows from a 1,001-row equal-time tenant, asserts the combined form/status cursor index, budgets adjacent 100-row pages below two seconds, excludes a later arrival and a foreign tenant, and exposes that arrival on refresh. Chromium loads seeded row 51 before quarantine/recovery and reruns WCAG A/AA scanning. |
| Lead-form definitions: `GET /api/lead-capture-forms` | Offset page; default 50 and maximum 100; maximum offset 50,000; exact filtered total; optional `all`, `active`, or `inactive` status | Active first, then update time and immutable ID descending. Settings exposes previous/next pages. Review and automation consumers request every status; landing-page and widget selectors request only active forms. Complete consumers ID-deduplicate bounded pages and verify one exact total | Malformed/non-positive/oversized/overflowing pages and invalid status return `400` before service work; direct callers repeat the bounds. Each page and its count share one read-only repeatable-read transaction. A total change or same-total row movement that creates overlap prevents the complete loader from claiming success; history beyond the explicit 50,100-addressable-row boundary fails visibly | Handler/library/route tests cover exact metadata, strict parsing, active selection, complete loading, total drift, overlap, stale-load rejection, and mutation reconciliation. Fresh PostgreSQL acceptance uses 1,001 tenant forms, asserts `idx_lead_capture_forms_org_active`, budgets active and adjacent pages below two seconds, proves stable repeat pages, exact submission counts, direct bounds, and foreign-tenant exclusion. Chromium starts with 52 forms, reaches row 51 on the second management page, and reruns WCAG A/AA scanning before using the complete catalog in automation and public capture. |
| Audit and operator work lists: `GET /api/audit-events`, `/api/admin/background-jobs`, `/api/workflow-automation-runs`, `/api/workflow-approvals` | Audit max 100/default 50; jobs max 200/default 50; workflow runs max 100/default 20 and the normal screen requests 25. Workflow approvals are a fixed 100-row actionable queue filtered to the current actor's active owner/admin role or current deal ownership. Every active definition is capped at 25 stored actions and reviewed approval plans expose one gate plus at most five tasks | Audit/jobs/runs use creation time then ID descending. Eligible pending approvals use request time then immutable ID ascending so the oldest decision is first. Workflow runs and their ordered immutable action outcomes are read in one repeatable-read transaction. Durable lead runs join only the exact same-tenant `workflow.lead_follow_up` job key; created-task links survive only a same-tenant task join | Invalid job filters are `400`; service caps protect non-HTTP callers. Approval eligibility is derived only from the session actor and current tenant roles/ownership; callers supply no collection selector, foreign approvals are absent, and only pending rows appear. A dead lead operation is shown as failed and retains audited replay. Complete audit, action, and approval history is available through the portable workspace package | Handler, overflow/export, workflow action/run/replay/approval UI, exact same-key/task-reference/approval foreign-tenant denial, role revalidation, rolling migrations, and real-PostgreSQL approval/lead/task acceptance. |
| Email/mailbox lists: `GET /api/email-messages`, `/api/me/email-messages`, `/api/shared-inbox/email-messages`, `/api/record-email-deliveries`, `/api/email-threads/{messageID}` | General/mailbox max 500/default 100; shared inbox first page 50/max 100 with continuation; entity timelines, record-delivery recovery, and threads fixed at 100 | General/mailbox use provider/creation time and ID; shared inbox orders open before closed, then effective received time and ID descending; threads use message chronology | Shared-inbox malformed/non-positive/oversized limits and malformed opaque cursors are `400` before service work. Its cursor retains the first-page snapshot plus bucket/time/ID position, so later arrivals and coordination changes wait for refresh instead of duplicating into older pages. Other caller limits outside the service boundary fall back to their safe default; entity/thread views deliberately return only their bounded recovery window | Handler/viewer and frontend continuation tests plus freshly migrated 1,001-row PostgreSQL equal-time, index-plan, direct-bound, mutation/new-arrival snapshot, final-page, privacy, and tenant evidence; Chromium loads row 51 and repeats WCAG A/AA scanning. SMTP/Gmail/Microsoft contract, uncertainty, and recovery evidence remain. |
| Email sequence enrollment history: `GET /api/email-sequence-enrollments?sequenceId=…` | First page defaults to 50 and explicit limits cap at 100; contact-scoped active enrollment lookup retains its existing focused unpaged shape | Opaque keyset cursor over immutable enrollment creation time then ID, both descending. Later enrollment/status/delivery changes do not move an existing row between historical pages; refresh reconciles mutable outcomes | Exactly one positive contact or sequence selector is required. Contact mode rejects pagination rather than ignoring it. Malformed/non-positive sequence selectors, cursors, and limits or limits above 100 return `400` before service work; the service repeats ID/cursor/limit validation. Settings loads only on drill-down, deduplicates continuation IDs, and discards closed/stale responses | Handler and UI tests cover selector ambiguity, bounds, disclosure, continuation, deduplication, and stale close/reopen. Fresh PostgreSQL acceptance uses 1,001 equal-time rows, asserts the tenant/sequence/creation/ID index, traverses adjacent and final pages, repeats the first page, rejects a direct oversized limit, exposes per-enrollment delivery outcomes, and excludes a foreign sequence. Chromium reconciles the real SMTP-sandbox acceptance with its individual finished enrollment before WCAG A/AA scanning. |
| Entity communications: `GET /api/calendar-events`, `/api/calls`, `/api/sms-messages` | Max 200/default 50 per exact tenant entity | Creation/start time then ID descending | Invalid entity scope is rejected; oversized/malformed limits fall back to the service default | Handler and service/provider tests. These fake-provider foundations remain hidden from production navigation where applicable. |
| Data-operation histories: `GET /api/imports`, `/api/data-operations/bulk`, `/api/data-operations/archive`, `/api/data-operations/duplicates`, `/api/workspace-exports`, `/api/crm-exports` | Imports/archive default 50/max 100; bulk default 20/max 100; duplicate review max 50; workspace export history fixed at 20; filtered CRM export history fixed at 30 | Creation/archive time and ID tie-breakers, or deterministic duplicate score/key order | Services cap every caller. Durable imports have an explicit 1,000-row/2 MiB boundary; synchronous CSV export refuses above 10,000 rows; durable filtered CSVs cap at 50,000 rows/50 MiB with seven-day artifacts; portable ZIPs cap at 50 MiB compressed | Handler and real-PostgreSQL job/rollback/merge/archive/export/tenant/performance acceptance. |
| Product catalog: `GET /api/product-catalog-items` | Offset page; default 50 and maximum 100; maximum offset 50,000; exact filtered total; optional literal name/SKU search of at most 100 characters and `all`, `active`, or `inactive` status | Active first, then case-insensitive name and immutable ID. Management exposes previous/next pages and quote preparation separately requests the complete active set, continuing only for legacy workspaces already above the new ceiling | Malformed/non-positive/oversized/overflowing pages and invalid search/status are `400` before database work; direct callers repeat the bounds. A namespaced transaction lock serializes writer authorization and the 100-active-item ceiling across API instances. Inactive history remains pageable but is never offered for a new quote | Handler/frontend tests cover query parsing, exact metadata, paging, search, mutation refresh, error semantics, the normal active-only quote request, and legacy active overflow retrieval. Fresh PostgreSQL acceptance uses 1,001 tenant rows, asserts the tenant/status/name index, budgets active and adjacent management pages below two seconds, proves literal wildcard search, tenant/role denial, duplicate handling, one-success/one-limit concurrent activation, and archive/reactivation recovery. Chromium loads row 51, searches it, creates an active service, excludes inactive history from selection, and carries the service into the finalized quote journey with WCAG A/AA coverage. |
| Quote templates: `GET /api/quote-templates` | Offset page; default 50 and maximum 100; maximum offset 50,000; exact filtered total; optional literal name search of at most 100 characters and `all`, `active`, or `inactive` status | Active first, then case-insensitive name and immutable ID. Management exposes previous/next pages; deal preparation separately requests and ID-deduplicates every active page, verifies a stable exact total, and continues only for legacy workspaces already above the new ceiling | Malformed/non-positive/oversized/overflowing pages and invalid search/status return `400` before service work; direct callers repeat the bounds. A tenant-namespaced transaction lock serializes active owner/admin revalidation and the 100-active-template ceiling across API instances. Archived history remains pageable but cannot enter quote preparation | Handler/frontend tests cover query parsing, exact metadata, paging, literal search, mutation refresh, error and drift semantics, active-only loading, and legacy overflow retrieval. Fresh PostgreSQL acceptance uses 1,001 tenant rows, proves indexed active selection, budgets active and adjacent management pages below two seconds, and covers literal wildcard search, tenant/role denial, a one-success/one-limit concurrent activation race, and archive/reactivation recovery. Chromium loads and searches row 51, creates the active pilot template, then carries it through finalization, approval, delivery, signature, and WCAG A/AA coverage. |
| Email templates and snippets: `GET /api/email-templates`, `/api/email-snippets` | Independent offset pages; default 50 and maximum 100; maximum offset 50,000; each returns an exact filtered total and accepts literal name search of at most 100 characters | Case-insensitive name then immutable ID. Settings exposes independent previous/next pages and rejects stale responses. Record composers separately request, ID-deduplicate, and verify every page so a legacy workspace above the new ceiling retains access to existing definitions | Malformed/non-positive/oversized/overflowing pages and overlong search return `400` before service work; direct callers repeat the bounds. One tenant-namespaced transaction lock revalidates the active writer, binds update/delete to the reviewed revision, and serializes separate 100-stored-template and 100-stored-snippet ceilings across API instances. Delete frees capacity; no definition is silently hidden | Handler/frontend tests cover strict parsing, exact metadata, independent paging/search, serialized mutation refresh, exact revisions, total drift, and legacy overflow. Fresh PostgreSQL acceptance seeds 1,001 rows in each catalog plus foreign sentinels, asserts the tenant/name indexes, budgets adjacent 50-row pages below two seconds, and covers literal wildcard search, tenant/role denial, exact-revision conflicts, transactional content-free lifecycle audit, concurrent final-slot races, and delete/recreate recovery. Chromium loads and searches row 51 in both catalogs before authoring and consuming the pilot template, with WCAG A/AA scanning. |
| Email sequence definitions: `GET /api/email-sequences` | Offset page; default 50 and maximum 100; maximum offset 50,000; exact filtered total; optional literal name search of at most 100 characters and `all`, `draft`, `active`, or `paused` status | Case-insensitive name then immutable ID. Management exposes previous/next pages; contact enrollment and nurture selectors separately request, ID-deduplicate, and verify every active page, preserving legacy active overflow above the new ceiling | Malformed/non-positive/oversized/overflowing pages and invalid search/status return `400` before service work; direct callers repeat the bounds. A tenant-namespaced transaction lock revalidates the active actor, binds update/delete/approval to the reviewed revision, and serializes the 100-active ceiling across API instances. Draft/paused history remains pageable but cannot enter new enrollment selection | Handler/frontend tests cover strict query parsing, exact metadata, paging/search, mutation refresh, reviewed-revision URLs, errors, active-only loading, total drift, and legacy overflow. Fresh PostgreSQL acceptance uses 1,001 tenant definitions plus a foreign sentinel, asserts indexed active selection, budgets active and adjacent pages below two seconds, and covers literal wildcard search, tenant/role denial, exact-revision conflicts, transactional lifecycle audit, a one-success/one-limit final-slot approval race, and pause/reapproval recovery. Chromium loads/searches row 51, creates and exact-revision approves the pilot cadence, excludes retained drafts from enrollment selection, delivers it through the SMTP sandbox, reconciles outcomes, and reruns WCAG A/AA coverage. |
| Workflow definitions: `GET /api/workflow-automations` | Offset page; default 50 and maximum 100; maximum offset 50,000; exact stored-definition total and exact workspace-wide active-action count | Active first, then configured position ascending, update time descending, and immutable ID descending. Settings exposes “Load more stored definitions,” deduplicates IDs, rejects stale responses, and returns to a fresh first page after every definition mutation | Malformed/non-positive/oversized/overflowing pages return `400` before service work; direct callers repeat the shared page bound. A repeatable-read transaction makes the page, stored total, and active-action summary one database snapshot. Inactive and unsupported legacy history remains pageable rather than silently discarded | Handler/frontend tests cover strict parsing, exact metadata, continuation, stale-load rejection, and mutation refresh. Fresh PostgreSQL acceptance seeds 1,001 tenant rows plus a foreign definition, asserts the management index, budgets adjacent 100-row pages below two seconds, proves stable repeat reads, exact totals and active-action summaries, direct bounds, and tenant exclusion. Chromium starts with 52 stored definitions, proves row 51 is absent from the first 50 and visible after continuation, then executes the reviewed deal rule. |
| Saved report definitions: `GET /api/report-definitions` | Offset page; default 50 and maximum 100; maximum offset 50,000; exact stored-definition total | Active first, then update time and immutable ID descending. Reports exposes “Load more stored report definitions,” deduplicates IDs, rejects stale responses, and returns to a fresh first page after every create or update | Malformed/non-positive/oversized/overflowing pages return `400` before service work; direct callers repeat the shared page bound. A repeatable-read transaction makes the page and stored total one database snapshot. Inactive and unsupported historical definitions remain pageable, while production continues to hide unsupported visualization families | Handler/frontend tests cover strict parsing, exact metadata, continuation, stale-load rejection, response identity, and mutation refresh. Fresh PostgreSQL acceptance seeds 1,001 tenant rows plus a foreign definition, asserts the management index, budgets adjacent 100-row pages below two seconds, proves stable repeat reads, exact totals, final/empty pages, direct bounds, and tenant exclusion. Chromium starts with 51 stored definitions, proves row 51 is absent from the first 50 and visible after continuation, then creates, runs, and exports the production report outcomes. |
| Saved views: `GET /api/saved-views?entityType=…` | Personal offset page for one allowlisted core entity; default 50 and maximum 100; maximum offset 50,000; exact same-snapshot total | Default first, then case-insensitive name and immutable ID. Normal list workspaces load every bounded page, ID-deduplicate it, reject total drift, and preserve legacy overflow before applying a view scope | Malformed/non-positive/oversized/overflowing pages or invalid entity types return `400`; direct callers repeat the bounds. Active writer membership and a tenant/user catalog lock serialize default transitions and the 100-stored-per-user/entity ceiling. Exact revisions prevent stale update/delete; delete recovers capacity | Handler/library/component tests cover pages, complete loading, drift, stale responses, overlapping mutations, exact revisions, stable errors, and successful-write/reload-failure disclosure. Fresh and rolling PostgreSQL acceptance covers 1,001 rows, exact totals, stable adjacent/repeat pages, the management index, tenant/user/role/entity/revision/default boundaries, and a concurrent final-slot race. Chromium loads row 51, creates/defaults/updates/deletes one definition, and repeats WCAG A/AA scanning. |
| Definitions and small workspace catalogs: users, pipelines/stages, custom fields, landing pages/widgets, audiences/scoring/campaign definitions, booking links, availability | Complete tenant-scoped catalog, currently unpaged | Every query has an explicit semantic order with an ID or unique-position tie-breaker | Pilot administration patterns keep these small today, but several definition families do not yet enforce a total stored-row ceiling. They must not become event/history stores. Hidden foundation catalogs are not maturity evidence | Handler and tenant tests cover current paths. Lead forms, saved views, email templates/snippets, sequence enrollment history, sequence-definition management, and quote-template management are no longer in this group and have bounded contracts above. Add explicit cursor/page contracts or enforced total ceilings before another production-like workload shows material growth, when historical rows become visible, or in the same slice that raises an approved active ceiling. |
| Users' sessions, notifications, record followers, quote approvals, invoices | Fixed security/work queues include notifications 50, quote approvals 100, and invoices 25. Sessions/followers are naturally team-bounded | Security or event time plus ID, or deterministic member order | The bounded queues serve a focused actionable/current set | Handler, lifecycle, role, tenant, and browser acceptance cover semantics. |
| Record notes and activity: detail embeds plus `GET /api/notes`, `/api/activities` | First page is 50 rows; explicit limits are capped at 100. Contact detail embeds its note first page, all four core details embed their activity first page, and every normal record workspace receives continuation metadata | Opaque keyset cursor over creation time then immutable ID, both descending. A cursor continues strictly below the last returned tuple, so new writes above it do not duplicate or skip older history | Malformed cursors, malformed/non-positive limits, and limits above 100 return `400` before service work; services repeat the bound. The UI exposes “Load older” only while `hasMore` is true and deduplicates IDs across requests | Handler and frontend tests cover parsing, disclosure, loading, stale-selection, and deduplication. Fresh PostgreSQL tests cover equal timestamps, a concurrent newer insert, multiple/final pages, direct-service overflow, and foreign-tenant exclusion; the pilot gate reads adjacent 100-row pages from 1,001 activities. |
| Synchronous CSV exports and PDFs/certificates | Core/audit/saved-report CSV refuses row 10,001; binary quote/certificate routes return one bounded immutable artifact | Export registry ordering is stable and formula-safe; artifacts are exact version/request lookups | Overflow is explicit and produces no partial success/audit evidence; database/report deadlines remain bounded | 10,000/10,001 real-PostgreSQL performance and overflow tests, browser download reconciliation, and quote artifact digest/header tests. |
| Singleton/detail/status/public lookup GET routes | One resource or bounded status document; core detail responses embed the bounded record-local history first pages described above | Exact tenant/token/resource lookup | Missing/foreign resources are non-disclosing; public routes have shared abuse budgets where applicable | Covered by `docs/security-surface-inventory.md` and `docs/tenant-isolation-matrix.md`; included in the GET digest so a route cannot be mistaken for a harmless singleton without review. |

## Findings and thresholds

- The material gap found by this audit was the four core list services accepting
  arbitrary positive page sizes and offsets. They now share one overflow-safe
  platform contract at both handler and service boundaries.
- Offset paging is retained deliberately. The maximum supported offset matches
  the largest currently advertised hosted record ceiling, page-number UX needs
  exact totals, and the real-PostgreSQL pilot gate remains far below budget.
- Catalogs and focused work queues are not evidence for large-dataset browsing.
  Their complete-list shape is a known pilot-scale constraint, not a promise of
  unlimited scale. Product catalog, quote templates, email-sequence definitions, workflow-definition management, and saved
  report-definition management are no longer in that unpaged group: each has exact bounded pages and visible
  continuation while retaining pageable inactive or legacy history. The
  product, quote-template, and email-sequence catalogs additionally have concurrency-safe
  100-active selection boundaries; email templates and snippets have separate
  concurrency-safe 100-stored-definition boundaries with complete legacy
  composer traversal.
- Standalone notes and contact/company/deal/task activity now use one bounded
  opaque cursor contract with visible older-history access. Company linked
  people now have a bounded first page plus explicit searchable continuation;
  stable unfiltered primary context is retained while a search result drives
  only the visible relationship list. Shared inbox has snapshot-bound visible
  continuation over its mutable open/closed queue, and lead-submission review
  has bounded creation-key continuation plus mutation-driven refresh. Workflow
  definition management now has bounded stable pages, an exact active-action
  summary, and mutation-driven first-page refresh; report definitions use the
  same bounded same-snapshot management shape without an action summary. The remaining reviewed
  growth decisions exclude lead-form, product, quote-template,
  email-template/snippet, email-sequence, workflow, and report catalogs and concern the
  other mutable definition catalogs without stored-row ceilings.
- New list endpoints must define tenant scope, stable total order with an ID
  tie-breaker, page/limit maximum, malformed-input behavior, overflow behavior,
  timeout, and handler plus PostgreSQL boundary tests before the GET digest is
  updated.

## Maintenance rule

When a GET route or collection query changes:

1. Reclassify its cardinality here and in the security surface inventory.
2. Test malformed, maximum, maximum-plus-one, empty-last-page, stable-order, and
   cross-tenant behavior proportional to the surface.
3. Use keyset pagination only when measured offset behavior, concurrent-write
   semantics, or an approved workload requires it; record the compatibility
   plan for existing page-number clients.
4. Update the expected GET count/digest only after the review and documentation
   are complete.
