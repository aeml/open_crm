# Open CRM Capability Matrix

Last reconciled: 2026-07-19

This is the canonical current-status view. `docs/professionalization-roadmap.md` remains the historical plan and implementation log; executable behavior and this matrix take precedence when its prose has drifted.

## Maturity definitions

| State | Meaning |
| --- | --- |
| `not started` | No meaningful executable product path exists. |
| `schema/API foundation` | Some persistence, service, API, or UI exists, but the promised user outcome is incomplete. |
| `usable with fake provider` | The workflow is demonstrable end to end only through an in-process fake or manual substitute. |
| `production-capable` | The local product path, failure behavior, permissions, and tests are implemented without relying on a fake external effect. Provider-backed features still need real-provider validation. |
| `validated with a real provider/pilot` | Current evidence proves the production path against a real provider or pilot workflow. |

No capability is promoted merely because a migration, definition editor, fake provider, or unit test exists.

## Product and platform

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| Password authentication and server sessions | `production-capable` | Argon2id, PostgreSQL sessions, secure production cookie, CSRF/origin checks, auth rate limit, handler/service tests, and bootstrap/logout/login browser acceptance | Add email verification, password reset, and session/device management. |
| Workspace bootstrap | `schema/API foundation` | Public bootstrap API and UI create an organization and owner session | Add email verification, abuse controls, idempotent provisioning, and trial lifecycle. |
| Roles and viewer write protection | `production-capable` | Owner/admin/member/viewer gates in API and UI, last-active-owner protection, route inventory gate, and forbidden-path tests | Extend only when pilot evidence requires custom permissions. |
| User invite and password setup | `production-capable` | One-time setup tokens, optional Postmark delivery, setup UI/tests, and invited-user activation browser acceptance against PostgreSQL | Add resend/revoke/expiry UX and production email verification. |
| User disable/reactivate/reassignment | `production-capable` | Admin UI/API, self/last-owner/cross-tenant protections, transactional active-work reassignment, session invalidation, follower/job/mailbox quiescence, preserved history, audit events, assignment guards, PostgreSQL acceptance, and browser reactivation journey | Validate with a pilot team and add lifecycle history/export to the broader tenant data package. |
| Tenant isolation | `production-capable` | Organization-scoped SQL, core cross-org handler tests, composite integrity constraints, and a route-set-digest-gated security inventory | Complete the explicit negative-path matrix for newer foundations and test every promoted surface. |
| Profile and preferences | `production-capable` | Profile/default landing APIs and UI plus persisted task/deal assignment and task-reminder choices; the browser journey proves task-reminder opt-out and re-enable | Add timezone only when scheduling behavior requires it. |
| In-app notifications | `schema/API foundation` | Persistence, unread center, preferences, durable meeting/task reminders, transactional idempotent task-assignment and mention/follower events, useful filters, and record deep links | Bring deal-assignment events to the same transactional/idempotent standard, add retention controls and event-quality observability, then validate noise with a pilot. |
| Audit events | `production-capable` | Admin audit list/filter UI and audited lifecycle actions | Inventory all sensitive/new mutations and add retention/export policy. |
| Plan catalog and entitlement display | `usable with fake provider` | In-code plans, usage counts, billing UI, limited create enforcement | Centralize enforcement across every API/worker path and reconcile hosted vs self-hosted behavior. |
| Stripe billing and hosted lifecycle | `not started` | Stripe variables are placeholders; provider resolves to an unconfigured stub | Implement Checkout/portal, subscriptions, signed webhooks, invoices, proration, dunning, reconciliation, suspension, and deletion/export. |
| Usage metering | `schema/API foundation` | Live seat/contact/deal counts and static limits | Add durable meters for messages, jobs, API/storage and explainable billing-period usage. |
| Public API, API tokens, and webhooks | `not started` | Browser-oriented internal REST routes only | Add versioned external surface, scoped tokens, signed delivery, retries, and integration logs after core reliability work. |

## Core CRM and team workflow

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| Contacts and companies | `production-capable` | Tenant-scoped CRUD/archive/restore, linking, core and typed custom-field filters, pagination, ownership, activity, custom-aware import/export, bounded reversible bulk operations, reviewed transactional duplicate merge including selected custom values, UI tests, and client/contact browser creation with cross-tenant denial | Validate restore discoverability and archive retention expectations with a pilot. |
| Deals and tasks | `production-capable` | CRUD/archive/restore with dependency safeguards, configurable stages/probabilities, assignment, close-date and exact due-window saved filters, stage-authoritative open/won/lost outcomes, required fixed close reasons plus optional notes/actor/time, reopen clearing with immutable outcome snapshots, close-aware exports, bounded reversible bulk archive/assignment, and transactionally generated follow-up tasks. Handler, UI, disposable-PostgreSQL, and browser acceptance cover tenant and invalid paths. | Complete the focused won-deal client handoff; validate close-reason vocabulary with a pilot before making it configurable. |
| Task reminders | `production-capable` | Exact overdue/rolling-24-hour/later/no-date surfaces and counts; preference-aware transactional assignment events; versioned durable due-soon/overdue jobs with revalidation, replay safety, activity evidence, and lifecycle/bulk/restore/automation synchronization; PostgreSQL, UI, and browser acceptance | Validate timing and notification noise with a pilot; add email delivery only if in-app reminders prove useful. |
| Notes and activity timelines | `production-capable` | Polymorphic validation, record timelines, explicit same-tenant active-member `@email` mentions, transactional activity/follow/notification writes, and PostgreSQL acceptance | Add an edit policy and retention behavior after pilot feedback. |
| Saved views | `production-capable` | Per-user persisted core and typed custom-field filters/defaults with URL and UI round-trip coverage | Validate defaults and performance with larger pilot datasets. |
| Record followers and mentions | `production-capable` | Organization-scoped idempotent subscriptions for contacts/companies/deals; active-member `@email` parsing; actor/mention auto-follow; transactional deduplicated notifications; viewer-safe UI; handler, PostgreSQL, and browser acceptance | Validate mention syntax and notification usefulness with a pilot; add bulk follower management only if demanded. |
| Focused team activity digest | `production-capable` | One notification-center view switches between followed records and tenant-wide work, supports bounded date and teammate filters, deep-links records, and has tenant-isolated PostgreSQL/UI/browser acceptance | Validate default scope/window with a pilot and add scheduled delivery only if the in-app digest proves useful. |
| Team usage reporting | `not started` | The focused digest and sales activity breakdown explain operational work, not employee adoption or product-usage reporting | Add privacy-respecting admin adoption summaries for active users and feature use only if a pilot decision requires them. |
| CSV export | `production-capable` | Tenant-scoped contact, company, deal, and task downloads preserve visible filters; active contact/company custom fields are stable columns. The synchronous path is load-tested at 10,000 rows, states that boundary in the UI, requests row 10,001 to detect overflow, and returns explicit `EXPORT_TOO_LARGE` instead of a partial file. The combined PostgreSQL browser journey checks all four files and excludes rolled-back/merged-away records. | Add a durable full-tenant portable offboarding bundle with users, configuration, history, attachments, and archived custom definitions; do not raise the synchronous memory ceiling. |
| CSV import | `production-capable` | Admin-only contact/company upload with common-header and active custom-field mapping suggestions, editable dry run, typed/required validation, 2 MiB/1,000-row bounds, duplicate skips, source-hash idempotency, 50-row durable progress checkpoints/resume, privacy-safe error CSV, audited history, and rollback that archives only unchanged imported records; handler, PostgreSQL, load, UI, and browser acceptance | Validate templates and mapping suggestions with a pilot; move above the synchronous ceiling to the durable job runner only if real files demand it. |
| Bulk operations | `production-capable` | Multi-select contacts, organization/individual clients, deals, and tasks; bounded archive/reassign plus contact/client status and task-completion writes; deal outcomes intentionally require an audited stage transition with close context. Strict all-or-nothing tenant resolution, active-member assignment checks, stable idempotency, serializable writes, persistent history, per-record activity, aggregate audit, changed-record-safe rollback, handler, disposable-PostgreSQL, UI retry/recovery, and browser acceptance cover the outcome. | Validate selection affordances and the 100-record ceiling with a pilot; add async execution only if observed list sizes require it. |
| Duplicate management | `production-capable` | Admin-only contact/client review detects exact normalized identity signals, explains candidate reasons and linked-work counts, and lets an operator choose the survivor plus every differing core or active custom value, including false/zero values. The source is archived; relationships are consolidated collision-safely; unselected target custom values are preserved; historical import/bulk/audit ledgers keep their original IDs; record versions reject stale reviews; stable idempotency makes retries safe; tenant-scoped history and audit remain. Handler, UI, disposable-PostgreSQL relationship/tenant/conflict tests, and the clean-schema browser journey cover the workflow. | Validate thresholds and false-positive review burden with a pilot; add an operator-assisted reconciliation tool only if permanent-merge mistakes prove common. |
| Custom fields | `production-capable` | Admin-managed, tenant-scoped contact/company definitions have stable keys, immutable types, bounded text/number/date/boolean/select values, create/edit required validation, list display, typed filters, saved-view and URL round trips, active-field import/export, selected duplicate merge, GIN indexing, audit events, and archive-with-value-retention semantics. Handler/unit/UI tests, a disposable-PostgreSQL vertical acceptance suite, and the clean-schema Chromium journey cover roles, malformed values, option conflicts, tenant isolation, persistence, and archive behavior. | Validate the 25-field ceiling and list/filter usefulness with a pilot; add definition restore/versioning only if accidental archive or schema evolution proves common. |
| Archive, restore, and retention | `production-capable` | A member-visible Archived Records workspace searches tenant-scoped contact/client/deal/task history; writers restore through a serializable transaction with activity/audit evidence; archived dependencies produce actionable conflicts; permanent duplicate-merge sources remain blocked history. Related notes, activity, and independently archived work are retained, no automatic hard delete runs, and handler/UI/disposable-PostgreSQL/browser acceptance covers recovery and forbidden paths. | Validate retention expectations with a pilot; implement hard deletion only through the Phase 3 tenant-offboarding policy and portable export workflow. |
| Data quality reports | `production-capable` | The live Reports view exposes exact tenant-scoped counts plus the first 25 actionable records for missing owners, missing contact details, stale deals with a 7–365-day threshold, incomplete open deals, unscheduled tasks, and an allowlisted services/construction/product-sales profile rule. Criteria and per-record reasons are explicit, archived rows are excluded, direct record links support cleanup, and handler/UI/disposable-PostgreSQL/browser tests cover thresholds, all profiles, tenant isolation, and navigation. | Validate rule usefulness and stale-window defaults with a pilot; add another fixed rule only when observed cleanup work justifies it. |
| Contact/client touchpoints and follow-up queues | `production-capable` | Viewer-aware tenant queries derive traceable touches from notes, durable task completions, completed calls, successful SMS, scheduled meetings, and visible email while excluding edits, failures, cancellations, reminders, and future due dates. No-touch records use creation time; client history includes currently linked people with source attribution; private email/meeting visibility, sent-email note deduplication, retained-owner filters, exact bounded queue counts, detail summaries, and report links have handler, focused UI, disposable-PostgreSQL, and clean-browser coverage. | Validate which sources, 30-day default, linked-person rollup, and per-viewer private-touch differences with a pilot; add a persisted override only if operators need to correct derived history. |
| Customer/client handoff | `schema/API foundation` | Business profile terminology and linked CRM history exist | Add won-deal conversion/handoff, client status, next owner, health/follow-up, and acceptance flow. |

## Communication, sales, and growth

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| System email | `production-capable` | Real Postmark adapter plus fake default and invite delivery seam | Verify current provider sandbox, add bounce/error observability, and setup/reset coverage. |
| Per-user send and mailbox sync | `production-capable` | Encrypted SMTP/IMAP credentials, Gmail and Graph ingestion/refresh, manual sync plus durable idempotent mailbox jobs, privacy links, and PostgreSQL acceptance coverage | Add provider acceptance tests, send via OAuth where applicable, richer conflict/recovery UX, and provider metrics. |
| Mailbox, tracking, and shared inbox | `schema/API foundation` | Message log/detail, open/click endpoints, private/shared inbound queue and assignment | Add consent/privacy controls, bounce/complaint events, threading/reply UX, abuse controls, and pilot validation. |
| Email templates/snippets | `production-capable` | Organization CRUD, merge catalog/rendering, record composer integration | Add preview/test-send and custom-field merge support. |
| Email sequences | `schema/API foundation` | Definitions, enrollment, reply completion, suppression, transactionally enqueued sends, leased claims, delivery ledger, dead-letter recovery, and explicit uncertain-SMTP resolution | Add approvals, bounce/reply analytics, deliverability controls, and real-provider validation. |
| Calls and SMS | `usable with fake provider` | Persistent history, consent/opt-out metadata, contact UI, fake providers | Implement real carrier calls/webhooks/delivery receipts, media retention, compliance, and sandbox tests or hide from pilot navigation. |
| Meetings and booking links | `usable with fake provider` | Events, availability, transactionally enqueued idempotent in-app reminders, booking-link definitions, and fake calendar provider | Add public slot booking, conflict checks, round robin, real calendar providers/webhooks, and guest notifications. |
| Pipeline configuration | `production-capable` | Admin UI and tenant-scoped serializable APIs create/rename/default up to 10 pipelines, create/rename/reclassify/reorder up to 20 unique stages, configure bounded open-stage probabilities while fixing won/lost at 100/0, retain stage IDs across edits, prevent outcome reclassification while any active or archived deal uses the stage, and emit audit history; role, conflict, cross-tenant, disabled-actor, PostgreSQL, UI, and browser continuity tests cover the outcome | Validate terminology and practical stage limits with a pilot; deletion remains intentionally absent because safe replacement/migration semantics are not yet specified. |
| Deal probability and forecasting | `production-capable` | Configurable audited stage probability drives period-bounded team, owner, unassigned, and stage rollups; unweighted/won/weighted values, close-date semantics, manual-FX omissions, and assumptions are explicit. Deal list/saved views/CSV share validated close-date filters. Unit, handler, disposable-PostgreSQL tenant/period/probability acceptance, focused UI, and clean-browser evidence cover the vertical flow. | Validate probability defaults, no-close-date policy, and quota operating cadence with a pilot; add immutable forecast snapshots only if pilot review requires point-in-time comparisons. |
| Sales activity reporting | `production-capable` | A viewer-safe live report counts deal creation/moves/won/lost, notes, and task creation/completion over a bounded UTC window with retained-teammate filtering, precise owner-vs-actor semantics, outcome win rate, stage entry/exit math, grouped won/lost reasons, retained close notes, and deal deep links. Transactional event-time snapshots preserve deal/pipeline/stage/outcome/reason meaning after edits; stage-ledger and close-reason coverage windows are disclosed separately. Handler, UI, clean-browser, and disposable-PostgreSQL snapshot/math/disabled-user/activity-link/cross-tenant acceptance cover the outcome; a mixed-history planner gate requires tenant/date and owner/date stage-event/activity indexes and excludes unrelated audit actions before aggregation. | Validate the default window, reason vocabulary, UTC-day presentation, rate definitions, and teammate usefulness with a pilot; add cohort/velocity metrics only with explicit event-time definitions. |
| Deal task automation rules | `production-capable` | Admin-managed tenant rules create exactly one bounded task on deal creation, a real stage change (optionally to one stage), or direct/bulk archive. Task, activity, idempotent run, and audit commit with the deal event; assignment uses the active deal owner or trigger-actor fallback. Broad unsupported definitions are hidden and safely skipped. UI/handler and disposable-PostgreSQL trigger/replay/tenant tests plus the clean-browser pilot journey cover the outcome. | Validate the three triggers, task wording, due-day range, and owner fallback with a pilot; keep broader workflow actions hidden until their own complete runtime slices. |
| Catalog, line items, and current quote PDF | `production-capable` | Tenant-scoped catalog/custom items snapshot saved name/SKU/type/price, calculate discount/tax/total, transactionally update deal value and activity, and render an authenticated current-data PDF. Focused UI, handler/unit, disposable-PostgreSQL calculation/snapshot/tenant tests, and the clean-browser journey cover the narrow proposal outcome. | Add immutable versioned quote records, terms/templates, delivery, approval, and explicit currency/expiry semantics in Phase 4. |
| Proposal tracking and e-signature | `schema/API foundation` | The production-capable Phase 2 placeholder records recipient, filename reference, and manually editable draft/sent/signed/declined/voided status with timestamps/activity. UI and PDF explicitly disclose that content is live, no proposal is sent, and no legal signature is collected. | Add real delivery and signing ceremony/provider, signed idempotent webhooks, signer access, immutable document/audit certificate, and closed-deal conversion before promoting e-signature. |
| Lead capture forms/pages/widgets | `schema/API foundation` | Public submission, hosted page and iframe widget create attributed contacts; bounded per-client read/write limits protect public routes | Add bot/spam challenges, consent, custom mapping, analytics, domains/SEO, and browser security coverage. |
| Audiences and lead scoring | `schema/API foundation` | Saved segment counts and manually invoked rule evaluation/routing | Add member drill-down, automatic scoring, simulation, exclusions, bulk rescore, SLA/round robin, and observability. |
| Marketing and nurture campaigns | `schema/API foundation` | Definitions, schedule metadata, audience snapshots, sequence binding | Add approvals, recipient expansion, durable compliant delivery, unsubscribe enforcement, exits/replies, and analytics. |

## Automation, analytics, and operations

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| General workflow definitions | `schema/API foundation` | Broad trigger/condition/action/timing/approval definitions and run metadata remain in tenant-scoped storage/APIs, but normal navigation deliberately exposes only the executable deal-task subset | Keep broad definitions hidden until record hydration, event capture, nested branching UX, validation previews, and negative-path coverage are complete. |
| General workflow execution | `schema/API foundation` | The bounded deal-task subset executes transactionally; other stored action types, conditions, schedules, approvals, and targets are not dispatched | Implement each broader family through durable jobs with idempotency, approvals, loop protection, retries, action attempts, replay, audit, and acceptance tests before exposure. |
| Custom report definitions | `schema/API foundation` | Object/field/filter/group/aggregate/chart metadata and builder UI | Add tenant-safe query compilation/execution, result tables/charts, limits, exports, sharing, and tests. |
| Dashboards and scheduled reports | `not started` | Fixed operational dashboard only | Add configurable widgets, layouts, scheduled delivery, and performance strategy after report execution. |
| Durable background jobs/outbox | `production-capable` | Tenant-scoped PostgreSQL queue with idempotency keys, leased `SKIP LOCKED` claims, capped retries, dead letters, admin replay/health UI, aggregate queue/lag/outcome metrics, graceful worker, and real-PostgreSQL restart/duplicate tests; meeting/task reminders, mailbox sync, and sequence sends use it | Add retention cleanup and move future automation/campaign/import work onto the same runner. |
| CI quality gates | `production-capable` | Exact Go 1.26.5/Alpine 3.24 and Node 24 toolchains, zero-reachable-vulnerability Go and zero-high-severity frontend audits, real-PostgreSQL backend tests, Chromium pilot journey, encrypted backup/restore acceptance, immutable deploy/rollback acceptance, migration compatibility policy, source-size no-growth ratchets, validated Prometheus alert rules, retained browser failure evidence, and deploy dependency on all four jobs | Add license, broader static-security, and accessibility gates. |
| Deployment and rollback | `production-capable` | CI-gated immutable commit-tagged image, exact public release verification, expand/contract migration enforcement, atomic release manifests, tested automatic failed-readiness recovery, guarded manual application rollback, and operator runbook | Exercise recovery on the production host, define image retention, and require backup/restore evidence before the first approved contract migration. |
| Backups and restore | `production-capable` | Pinned Restic path creates and verifies custom dumps, encrypts snapshots, applies retention/checks, records atomic success/failure evidence, restores into isolated PostgreSQL, forward-migrates, sanity-checks, and gates deploys through CI; daily/weekly systemd templates reject local production repositories | Provision approved off-host credentials, enable timers on the production host, connect freshness/failure alerts, and record a real-repository drill before pilot validation. |
| Observability and alerting | `schema/API foundation` | Redacted structured route/request/job/provider logs; token-protected bounded-route HTTP latency/error, database, aggregate queue, worker, Postmark/SMTP, and backup/restore Prometheus metrics; promtool-validated alert rules; tenant job recovery UI; initial SLO/runbooks | Configure production scrape credentials and an approved Alertmanager destination, prove a synthetic alert and backup/provider incidents, then validate SLOs/retention against pilot traffic. |
| Performance and failure testing | `schema/API foundation` | Real-PostgreSQL gates seed 12 representative tenants for core read/write latency plus mixed multi-tenant sales event/audit history; they assert core and sales-report tenant/date and owner/date index plans, exact totals, 96 concurrent mixed reads, 32 transactional cross-tenant writes, wrong-tenant denial, bounded exhausted/locked/closed database failures and recovery, a tenant-isolated 10,000-row export under 5 s, and a mapped/deduplicated 1,000-row import under 10 s. Postmark `503`/deadline recovery and ambiguous SMTP quarantine are tested; production builds enforce entry/lazy/total/CSS raw+gzip budgets. | Retain the same evidence from an approved production-like host run and add provider/feature loads as remaining Phase 3 outcomes converge. |
| Browser end-to-end tests | `production-capable` | Playwright/Chromium CI gate drives workspace bootstrap, task-reminder preference persistence, invited-user lifecycle, required custom fields, mapped import/rollback, client/contact creation and reviewed duplicate merge, pipeline probability/rename continuity and forecast, calculated line items plus manual proposal tracking/current-PDF download, task automation/reminders, touchpoints/follow-up, data quality, mentions/following, won close reason/notes plus report/export reconciliation, reversible client bulk status, task archive/restore, all core exports, persistence, and two-context tenant/contact/quote/deal-close isolation against PostgreSQL 16. | Extend the same journey through handoff, versioned quote delivery/signing, full tenant offboarding export, hosted billing, and production-provider sandboxes as those outcomes converge. |
| Frontend route loading/maintainability | `schema/API foundation` | Shared API/UI primitives, route-level lazy loading, and CI-gated entry/lazy/total raw+gzip and source-size no-growth budgets exist; current measured entry/largest-lazy/aggregate assets are 176.85/48.01/625.52 KiB raw and 57.66/12.42/199.09 KiB gzip. Custom-field, archive recovery, and pipeline settings remain isolated routes; close-review UI is a 73-line seam, Reports is 28.38/7.27 KiB, and the shared touchpoint panel remains separate. Tested splits leave contacts at 1,298 lines, companies at 998, deals at 1,065, tasks at 839, and the focused automation route at 261; tight per-file ratchets preserve the gains. | Extract the next contact/company orchestration seam before either reaches its current no-growth ceiling, while preserving route and bundle budgets. |

## Deferred categories

AI, help desk/tickets, marketplace/custom objects, native mobile, real-time collaboration, and enterprise breadth are outside convergence scope. They must not be exposed or started until pilot evidence changes `docs/product-vision.md` through an explicit product decision.
