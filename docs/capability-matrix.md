# Open CRM Capability Matrix

Last reconciled: 2026-07-21

This is the canonical current-status view. `docs/professionalization-roadmap.md` remains the historical plan and implementation log; executable behavior and this matrix take precedence when its prose has drifted.

Phase 3 decisions, credentials, and operator-owned evidence that cannot be completed through repository changes alone are centralized in `docs/phase-3-decision-register.md`.

## Maturity definitions

| State | Meaning |
| --- | --- |
| `not started` | No meaningful executable product path exists. |
| `schema/API foundation` | Some persistence, service, API, or UI exists, but the promised user outcome is incomplete. |
| `usable with fake provider` | The workflow is demonstrable end to end only through an in-process fake or manual substitute. |
| `production-capable` | The local product path, failure behavior, permissions, and tests are implemented without relying on a fake external effect. Provider-backed features still need real-provider validation. |
| `validated with a real provider/pilot` | Current evidence proves the production path against a real provider or pilot workflow. |

No capability is promoted merely because a migration, definition editor, fake provider, or unit test exists.

## Product and platform

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| Password authentication and server sessions | `production-capable` | Argon2id, PostgreSQL sessions, secure production cookie, CSRF/origin checks, verified-email gating, invited-user activation, and public self-service password recovery. My Profile lists the signed-in user's active server-side sign-ins using workspace and timing only, identifies the current one, and confirms audited individual or all-other revocation without collecting or exposing tokens, addresses, or browser fingerprints. Those recovery writes remain available during hosted suspension. Login locks the global credential row through session commit, while reset completion uses the same lock and a statement-current session deletion, so a forced concurrent old-password login cannot survive the reset. Reset requests remain generic with a shared 5/client/hour abuse budget, persisted five-minute recipient cooldown, one-hour token digests, recoverable provider delivery state, and production-hidden fake links. Postmark bounce/complaint callbacks bind to a separate attempt key rather than the reset credential; bounces become safely retryable failures and complaints globally suppress future system mail. Handler/email/UI tests, disposable-PostgreSQL eligibility/cooldown/expiry/replay/delivery-recovery and session ownership/current/expiry/idempotency/audit acceptance, the forced race, authenticated webhook exact-attempt/privacy/tamper acceptance, aggregate metrics/alerts, WCAG scans, and the clean browser manual-plus-reset multi-session journey cover the outcome. | Validate signup, invite, and reset through the approved live Postmark sender. |
| Workspace bootstrap | `production-capable` | Public UI/API transactionally provision one owner, workspace, and default pipeline under an advisory-locked retry key plus secret-safe request fingerprint. No session or trial clock exists before a one-time 24-hour email link is consumed; verification atomically starts the 14-day trial, writes audit evidence, and creates the owner session. The Postmark adapter and fake provider, same-key delivery recovery, unique-email/slug handling, generic persisted-cooldown resend, shared PostgreSQL 3/client/hour signup and 10/client/minute verification limits, handler tests, disposable-PostgreSQL lifecycle acceptance, and the clean browser journey cover the outcome. | Validate one full signup through the approved live Postmark sender. |
| Roles and viewer write protection | `production-capable` | Owner/admin/member/viewer gates in API and UI, last-active-owner protection, route inventory gate, and forbidden-path tests | Extend only when pilot evidence requires custom permissions. |
| User invite and password setup | `production-capable` | One-time digest-only setup tokens, seven-day expiry, Postmark/fake delivery, and setup completion that atomically records email verification. Each delivery has a separate digest-only correlation key and provider message reference. Authenticated Postmark bounce/complaint callbacks are durable, idempotent, tenant- and attempt-bound, retain no recipient/content/payload, surface delivery state to admins, keep bounce retry/revoke recovery available, and block resend after a spam complaint. Admins can see pending/expired/revoked state, rotate an invitation through resend, and explicitly revoke it; revocation disables access, invalidates sessions, quiesces effects, preserves history, and is safely repeatable. Production and Postmark responses never expose the raw setup token, delivery key, or link. Handler/UI tests, freshly migrated PostgreSQL acceptance for cross-tenant denial, expiry, token/delivery-key rotation, provider races, old-link rejection, webhook duplicates/tamper/stale attempts, complaint suppression, revocation, reactivation, audit counts, and the clean browser lifecycle cover the outcome. | Validate invitation delivery and feedback through the approved live Postmark sender. |
| User disable/reactivate/reassignment | `production-capable` | Admin UI/API, self/last-owner/cross-tenant protections, transactional active-work reassignment, session invalidation, follower/job/mailbox/quote-delivery quiescence, preserved history, audit events, assignment guards, bounded whole-transaction retries for session-touch serialization/deadlock conflicts, deterministic PostgreSQL conflict acceptance, and browser reactivation journey. Quote delivery preparation and claims revalidate and lock the active sender membership before durable intent/effect state, so deactivation cannot be followed by a late prepared intent, marks existing unsent intent failed, and quarantines an in-flight provider outcome as uncertain. | Validate with a pilot team and add lifecycle history/export to the broader tenant data package. |
| Tenant isolation | `production-capable` | Organization-scoped SQL, composite integrity constraints where stable relations permit them, a 246-route digest-gated security inventory, non-disclosing core handler tests, and a 25-row digest-gated real-PostgreSQL evidence matrix for every promoted service family. The core database acceptance proves tenant-scoped list/get/update/archive/delete behavior plus foreign relationship, stage, entity, actor, assignee, note, saved-view, and saved-report execution/export rejection with no partial effects. Company-linked-contact writes reject a missing, archived, or foreign ID instead of silently accepting a partial relationship set. The two-workspace Chromium journey separately verifies foreign contact/follower/touchpoint/quote/close/export/report requests return `404`. | Extend `docs/tenant-isolation-matrix.md` in the same slice as every future promotion and validate the boundary under a pilot's real role/data patterns. Hidden foundations remain ineligible for promotion until they add proportional PostgreSQL and forbidden-path evidence. |
| Profile and preferences | `production-capable` | Profile/default landing APIs and UI plus persisted task/deal assignment and task-reminder choices; the browser journey proves task-reminder opt-out and re-enable | Add timezone only when scheduling behavior requires it. |
| Adaptive business profiles | `production-capable` | General, Services, Product Sales, and Construction Services profiles adapt core company/deal/task vocabulary without changing tenant or workflow semantics. Services/construction expose Jobs as the mature pipeline record and Service/Site Tasks as linked delivery work; API module lists contain only implemented surfaces. | Add another profile or a separate delivery record only when approved pilot evidence shows the core record model cannot express the work. |
| In-app notifications | `schema/API foundation` | Persistence, unread center, preferences, durable meeting/task reminders, and transactional idempotent task-assignment, deal-assignment, mention, and follower events with useful filters and record deep links. Direct deal create/update, bulk reassignment/rollback, and member-lifecycle reassignment share a monotonic owner generation and notification transaction. Startup/hourly `SKIP LOCKED` retention keeps read rows for 90 days and unread rows for 365 days; protected aggregate metrics expose backlog age, reviewed event mix, recipient concentration, and cleanup outcomes without customer labels, with validated alerts and PostgreSQL acceptance for lifecycle boundaries, idempotency, cross-tenant aggregation, unknown-event bucketing, and concurrent cleaners. The browser journey proves recipient visibility and deal navigation. | Validate event selection, the provisional 100-event recipient warning, and perceived noise with a pilot before promotion. |
| Audit events | `production-capable` | Organization-scoped append-only history is retained for the workspace lifetime and can be removed only by deleting the parent workspace under an approved tenant-deletion policy. PostgreSQL rejects updates, deletes, truncation, non-object metadata, and top-level secret-like metadata keys; the service also strips those keys before insert and the read model accepts typed/nested JSON. Owners/admins can review exact event filters and download an oldest-first, formula-neutralized CSV that explicitly refuses more than 10,000 matches, records its own download, and remains tenant scoped. The complete portable workspace ZIP includes `audit_events.ndjson`. A digest-gated inventory currently covers all 43 producer source files, including transactional saved-report definition and download events. Handler/unit/UI tests, freshly migrated PostgreSQL tenant/immutability/mixed-JSON/secret/overflow/cascade acceptance, portable-export assertions, WCAG coverage, and the clean browser journey cover the outcome. | Validate event vocabulary, audit usefulness, and workspace-lifetime retention expectations with a pilot; make the eventual Phase 3 tenant-deletion workflow consume the same documented deletion boundary. |
| Plan catalog and entitlement display | `usable with fake provider` | In-code plans, live seat/contact/deal counts, billing UI, and fail-closed hosted seat/contact/deal limits. Migration `078_billing_capacity_reservations.sql` closes check-then-write races with tenant-scoped expiring claims; claims include concurrent work and are consumed under the organization lock in the same transaction as direct contact/deal/user creation, user reactivation, contact import completion, public lead capture, and single/bulk contact/deal restore. Limit, failure, expiry, consume/cancel, tenant-isolation, and concurrent last-slot behavior are tested against disposable PostgreSQL. Only a Stripe-configured runtime is managed; fake/self-hosted mode reports `unmanaged`, presents counts as unlimited, hides hosted pricing controls, and bypasses trial/subscription/limit enforcement across private routes, public lead capture, and workers. The current hosted comparison names only enforced capacity, treats Stripe Checkout as price authority, and returns no feature grants until a commercial feature contract is approved, so incomplete API/SSO/automation/reporting foundations are not sold as active benefits. One route-derived policy covers every registered managed tenant mutation, including the credential-writing OAuth callback; the route-wide guard proves new writes cannot silently bypass it. Authentication carries the same server-derived access decision into persistent UI state, where shared role-aware controls hide or disable managed mutations but preserve reads and recovery. | Approve the feature/tier contract, then enforce only the approved feature or period-meter outcomes consistently across hosted API, UI, and workers while keeping self-hosted core data unrestricted. |
| Stripe billing and hosted lifecycle | `schema/API foundation` | Backend-hosted Checkout and customer-portal sessions, tenant-scoped durable Checkout idempotency, a raw-body HMAC/replay-window webhook boundary, event deduplication/tamper detection, ordered subscription and invoice ledgers, provider-observed trial/dunning/suspension/cancellation state, and six-hour provider-API reconciliation through the leased PostgreSQL queue. Reconciliation validates tenant/customer/subscription metadata, applies only state older than the webhook cursor, refreshes the recent 25-invoice window, exposes freshness, records failures/audit/provider telemetry, retries safely, and dead-letters into the admin Operations replay workflow. Owners/admins can read a suspension-safe newest-first 25-invoice view with provider status, currency-correct amounts, payment attempts/next retry, paid time, and sanitized HTTPS invoice/PDF links; Stripe remains authoritative for retry timing. In Stripe mode, suspended tenant mutations return `402` only after authentication/role checks, entitlement-read failures return `503`, public lead capture fails closed without tenant disclosure, and non-billing tenant workers defer durably every 15 minutes without spending attempts; billing reconciliation and workspace portability remain exempt for recovery. Login, verification, and current-session responses expose a non-blocking managed `writable`/`read_only`/`unavailable` or self-hosted `unmanaged` snapshot. A global banner and shared controls immediately enter managed read-only mode after a policy rejection while keeping normal navigation, reads, exports, billing recovery, and Operations replay available. Route-wide, worker, public-path, handler, UI, and disposable-PostgreSQL tests cover both managed and self-hosted boundaries. A credential-free Stripe HTTP contract acceptance drives the real app routes and PostgreSQL through trial entitlements, idempotent Checkout, invalid/duplicate signed webhooks, authoritative activation, queued subscription/invoice reconciliation, suspension-safe tenant invoice visibility, writable past-due grace, unpaid suspension and portal recovery, reactivation, scheduled cancellation, and final read-only cancellation. A separate isolated Chromium journey provisions and verifies a hosted workspace through the UI, reaches the exact server-created Checkout/Portal destinations, proves a browser return cannot activate the plan, replays an exact signed event, verifies direct-write enforcement and WCAG states through suspension/recovery/cancellation, and downloads the portable export while read-only. The original self-hosted journey still runs separately in fake/unmanaged mode. | Run an approved credentialed Stripe test-mode smoke as deployment evidence; define explicit portal, proration, and resubscription policy, enforce features and period meters, automate dunning timing, then implement tenant deletion and retention policy. |
| Usage metering | `schema/API foundation` | `GET /api/billing/usage` and an exempt once-daily `billing.usage.snapshot` job reconcile one repeatable-read tenant snapshot from authoritative records and upsert retained evidence per workspace/period. The scheduler uses a UTC-day idempotency generation, skips current/active work, batches discovery, retries/dead-letters through the shared leased queue, and remains operable during hosted suspension; bounded serialization retries cover concurrent browser/worker observations. It reports active seats, contacts, and deals; period sent outbound email; period successful workflow runs and durable jobs; and current estimated PostgreSQL row bytes across dynamically discovered tenant-scoped base tables. Exact Stripe period start/end is persisted from signed webhooks/provider reconciliation and used when available; all other workspaces use the current UTC month. Plan & Billing exposes period/basis, observation time, units, and source definitions, while Operations exposes failed-job replay. Disposable-PostgreSQL acceptance covers inclusion/exclusion edges, concurrent update-in-place retention, daily scheduling/dedupe/worker evidence, calendar fallback, source-table discovery, and foreign-tenant isolation. Ephemeral capacity claims and retained billing snapshots are excluded from storage accounting. This is measurement, not a newly invented quota; self-hosted remains unrestricted, API use is omitted because no versioned external API exists, and row bytes exclude indexes/external object storage. | Approve the hosted plan/feature contract before turning message/automation/job/storage evidence into limits; apply the same transactional effect/claim invariant to any approved quota. Add API-use metering only with the real versioned external API. |
| Public API, API tokens, and webhooks | `not started` | Browser-oriented internal REST routes only | Add versioned external surface, scoped tokens, signed delivery, retries, and integration logs after core reliability work. |

## Core CRM and team workflow

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| Contacts and companies | `production-capable` | Tenant-scoped CRUD/archive/restore, linking, core and typed custom-field filters, pagination, ownership, activity, custom-aware import/export, bounded reversible bulk operations, and reviewed transactional duplicate merge including selected custom values. Contacts and Clients have distinct normal-navigation workspaces so unqualified captured leads remain discoverable and assignable before client conversion. Creating a new person from an organization client reserves contact capacity and commits the non-client contact, typed values, primary-safe company link, company timestamp, and both activity records in one transaction. Contact and company selection now abort obsolete loads and distinguish repeated A-to-B-to-A visits; snapshot writes serialize per visit, duplicate notes/tasks are suppressed, returned record/work identities are validated, and late saves or work cannot replace a newer detail. Successful company creation keeps its complete server snapshot instead of issuing a redundant read. Forced-link rollback, wrong-tenant, role, capacity, duplicate, delayed-response UI, public-capture assignment, and clean-browser acceptance prevent orphan contacts and cross-record response leaks. | Validate restore discoverability, archive retention expectations, and record-switch behavior with a pilot. |
| Deals and tasks | `production-capable` | CRUD/archive/restore with dependency safeguards, configurable stages/probabilities, assignment, close-date and exact due-window saved filters, stage-authoritative open/won/lost outcomes, required fixed close reasons plus optional notes/actor/time, required account relationships for won outcomes, reopen clearing with immutable outcome snapshots, close-aware exports, bounded reversible bulk archive/assignment, transactionally generated follow-up tasks, and idempotent won-deal client promotion. Deal selection aborts obsolete requests and uses visit identity to reject late A-to-B-to-A loads; edits, stage moves, quote lines, and quote-signature evidence serialize snapshot-changing operations per deal visit, while duplicate notes/tasks are suppressed. Every returned identity is validated, only the intended directory record is updated, and no result can replace a newer detail. Task quick-complete/reopen/assignment and full-form saves have equivalent per-record pending and identity contracts; a save cannot replace a newer task or navigate after the task route unmounts. Handler, focused delayed-response UI tests, disposable-PostgreSQL, and browser acceptance cover tenant and invalid paths. | Validate close-reason vocabulary and handoff expectations with a pilot before adding configuration or additional automation. |
| Task reminders | `production-capable` | Exact overdue/rolling-24-hour/later/no-date surfaces and counts; preference-aware transactional assignment events; versioned durable due-soon/overdue jobs with revalidation, replay safety, activity evidence, and lifecycle/bulk/restore/automation synchronization; PostgreSQL, UI, and browser acceptance | Validate timing and notification noise with a pilot; add email delivery only if in-app reminders prove useful. |
| Notes and activity timelines | `production-capable` | Polymorphic validation, record timelines, explicit same-tenant active-member `@email` mentions, transactional activity/follow/notification writes, and PostgreSQL acceptance | Add an edit policy and retention behavior after pilot feedback. |
| Saved views | `production-capable` | Per-user persisted core and typed custom-field filters/defaults with URL and UI round-trip coverage | Validate defaults and performance with larger pilot datasets. |
| Record followers and mentions | `production-capable` | Organization-scoped idempotent subscriptions for contacts/companies/deals; active-member `@email` parsing; actor/mention auto-follow; transactional deduplicated notifications; viewer-safe UI; and record-scoped follower loading that resets on navigation, validates the returned entity, and rejects late responses from an earlier record. Handler, delayed-response UI, PostgreSQL, and browser acceptance cover the workflow. | Validate mention syntax and notification usefulness with a pilot; add bulk follower management only if demanded. |
| Focused team activity digest | `production-capable` | One notification-center view switches between followed records and tenant-wide work, supports bounded date and teammate filters, deep-links records, and has tenant-isolated PostgreSQL/UI/browser acceptance | Validate default scope/window with a pilot and add scheduled delivery only if the in-app digest proves useful. |
| Team usage reporting | `not started` | The focused digest and sales activity breakdown explain operational work, not employee adoption or product-usage reporting | Add privacy-respecting admin adoption summaries for active users and feature use only if a pilot decision requires them. |
| CSV export | `production-capable` | Tenant-scoped contact, company, deal, and task downloads preserve visible filters; active contact/company custom fields are stable columns. The synchronous path is load-tested at 10,000 rows, states that boundary in the UI, requests row 10,001 to detect overflow, and returns explicit `EXPORT_TOO_LARGE` instead of a partial file. The combined PostgreSQL browser journey checks all four files and excludes rolled-back/merged-away records. | Retain these bounded filtered operational downloads; use the portable workspace export for complete offboarding rather than raising the synchronous memory ceiling. |
| Workspace portability export | `production-capable` | Owners/admins can request an idempotent full-workspace ZIP from Plan & Billing even while hosted writes are suspended. A leased durable job reads one repeatable-read PostgreSQL snapshot and emits a manifest plus NDJSON for every explicitly classified tenant business table, including archived records and definitions. It redacts auth/session/token/password/provider-secret/Stripe-sensitive material, excludes private mailbox content and unsupported attachment bodies, refuses newly added unclassified tenant tables, caps bundles at 200 MiB uncompressed/50 MiB compressed with a 10 MiB row ceiling, allows only one active generation and three retained ready files per workspace, records counts and SHA-256, audits request/ready/download, and clears artifact bytes after seven days while retaining metadata. Handler, UI, disposable-PostgreSQL generation/redaction/expiry/cross-tenant tests, and the clean browser journey cover the outcome. | Validate the documented interchange package with a pilot and add an operator-streamed path only if a real tenant exceeds the bounded self-service ceiling. |
| CSV import | `production-capable` | Admin-only contact/company upload with common-header and active custom-field mapping suggestions, editable dry run, typed/required validation, 2 MiB/1,000-row bounds, duplicate skips, source-hash idempotency, 50-row durable progress checkpoints/resume, privacy-safe error CSV, audited history, and rollback that archives only unchanged imported records; handler, PostgreSQL, load, UI, and browser acceptance | Validate templates and mapping suggestions with a pilot; move above the synchronous ceiling to the durable job runner only if real files demand it. |
| Bulk operations | `production-capable` | Multi-select contacts, organization/individual clients, deals, and tasks; bounded archive/reassign plus contact/client status and task-completion writes; deal outcomes intentionally require an audited stage transition with close context. Strict all-or-nothing tenant resolution, active-member assignment checks, stable idempotency, serializable writes, persistent history, per-record activity, aggregate audit, changed-record-safe rollback, handler, disposable-PostgreSQL, UI retry/recovery, and browser acceptance cover the outcome. | Validate selection affordances and the 100-record ceiling with a pilot; add async execution only if observed list sizes require it. |
| Duplicate management | `production-capable` | Admin-only contact/client review detects exact normalized identity signals, explains candidate reasons and linked-work counts, and lets an operator choose the survivor plus every differing core or active custom value, including false/zero values. The source is archived; relationships are consolidated collision-safely; unselected target custom values are preserved; historical import/bulk/audit ledgers keep their original IDs; record versions reject stale reviews; stable idempotency makes retries safe; tenant-scoped history and audit remain. Handler, UI, disposable-PostgreSQL relationship/tenant/conflict tests, and the clean-schema browser journey cover the workflow. | Validate thresholds and false-positive review burden with a pilot; add an operator-assisted reconciliation tool only if permanent-merge mistakes prove common. |
| Custom fields | `production-capable` | Admin-managed, tenant-scoped contact/company definitions have stable keys, immutable types, bounded text/number/date/boolean/select values, create/edit required validation, list display, typed filters, saved-view and URL round trips, active-field import/export, selected duplicate merge, GIN indexing, audit events, and archive-with-value-retention semantics. Handler/unit/UI tests, a disposable-PostgreSQL vertical acceptance suite, and the clean-schema Chromium journey cover roles, malformed values, option conflicts, tenant isolation, persistence, and archive behavior. | Validate the 25-field ceiling and list/filter usefulness with a pilot; add definition restore/versioning only if accidental archive or schema evolution proves common. |
| Archive, restore, and retention | `production-capable` | A member-visible Archived Records workspace searches tenant-scoped contact/client/deal/task history; writers restore through a serializable transaction with activity/audit evidence; archived dependencies produce actionable conflicts; permanent duplicate-merge sources remain blocked history. Related notes, activity, and independently archived work are retained, no automatic hard delete runs, and handler/UI/disposable-PostgreSQL/browser acceptance covers recovery and forbidden paths. The portable workspace package provides an audited pre-deletion export. | Validate retention expectations with a pilot; implement hard deletion only through an explicitly approved Phase 3 tenant deletion and retention policy. |
| Data quality reports | `production-capable` | The live Reports view exposes exact tenant-scoped counts plus the first 25 actionable records for missing owners, missing contact details, stale deals with a 7–365-day threshold, incomplete open deals, unscheduled tasks, and an allowlisted services/construction/product-sales profile rule. Criteria and per-record reasons are explicit, archived rows are excluded, direct record links support cleanup, and handler/UI/disposable-PostgreSQL/browser tests cover thresholds, all profiles, tenant isolation, and navigation. | Validate rule usefulness and stale-window defaults with a pilot; add another fixed rule only when observed cleanup work justifies it. |
| Contact/client touchpoints and follow-up queues | `production-capable` | Viewer-aware tenant queries derive traceable touches from notes, durable task completions, completed calls, successful SMS, scheduled meetings, and visible email while excluding edits, failures, cancellations, reminders, and future due dates. No-touch records use creation time; client history includes currently linked people with source attribution; private email/meeting visibility, sent-email note deduplication, retained-owner filters, exact bounded queue counts, detail summaries, and report links have handler, focused UI, disposable-PostgreSQL, and clean-browser coverage. | Validate which sources, 30-day default, linked-person rollup, and per-viewer private-touch differences with a pilot; add a persisted override only if operators need to correct derived history. |
| Client health signals | `production-capable` | The Clients view and client detail summary derive Healthy, Watch, or Needs attention from viewer-visible follow-up plus overdue/due-soon open tasks. Company health rolls up direct account and currently linked-person tasks/touches; individual-client health stays contact-scoped. Exact reasons, thresholds, task counts, source links, owner/status/type filters, private-touch variance, retained owners, empty totals, and the deliberate absence of an issue signal are covered by handler, focused UI, disposable-PostgreSQL, and clean-browser acceptance. Migration 71 adds bounded partial tenant/entity and customer-list query indexes. | Validate thresholds, Watch timing, reason language, and private-touch expectations with a pilot; add a persisted manual override or issue signal only if an operator workflow and source of truth justify it. |
| Client review and renewal obligations | `production-capable` | One optional schedule per active customer company or individual client creates/reschedules an ordinary assigned task transactionally with its reminder state. One-time and 1/3/6/12-month cadence, next-future-period advancement, replay safety, direct task rescheduling, one-time reopen, deliberate clear, activity/audit evidence, dashboard due buckets, tenant isolation, and blockers against orphaning archive/demotion/merge/bulk mutations are covered by handlers, focused UI, disposable-PostgreSQL, and the clean browser recurrence journey. Migration 72 adds composite tenant/task integrity and bounded active-due access. The UI and API explicitly state this is follow-up metadata, not billing or a legal renewal. | Validate cadence choices, dashboard horizon, task wording, and ownership practice with a pilot; add calendar linkage only if the ordinary task workflow proves insufficient. |
| Adaptive service/job tracking | `production-capable` | Services/construction profiles present the production-capable deal as a Job with client, primary contact, owner, value, target date, pipeline status, notes, tasks, activity, archive/restore, saved views, and export; linked Service/Site Tasks represent delivery work without a second project model. Focused adaptive-label tests and the underlying PostgreSQL/browser relationship coverage apply unchanged. | Validate terminology and whether any delivery state cannot be represented by job stages plus tasks before considering a separate project family. |
| Customer segment views | `production-capable` | Ordinary client views persist search, owner/unassigned, and typed company custom-field filters; separately scoped health segments persist client type, derived health, stale threshold, and retained owner without mixing into or replacing ordinary-list defaults. Tenant/user-scoped saved-view persistence, focused UI tests, and health-query PostgreSQL coverage support the outcome. | Validate useful default segments with a pilot; do not add tags or marketing behavior without a demonstrated customer workflow. |
| Customer/client handoff | `production-capable` | A won transition requires a live company or primary-contact relationship and atomically promotes its explicit active company to customer; only a company-less win promotes the primary contact to customer/individual-client, avoiding duplicate account records. Create, stage-transition, and later edits enforce that invariant with actionable API/UI feedback; legacy won deals can be repaired by linking an account through the same idempotent rule. Reopening never demotes the client, and activity/audit metadata names the source deal. Migration 70 reconciles legacy wins without fabricating actors and adds account-list access paths. Won deal detail links directly to the account, whose company/contact page summarizes won records, close reasons, open account tasks, recent shared notes, and key linked contacts; direct status changes add exact before/after account activity. PostgreSQL transition/create/unlink/late-link/reopen/backfill/status/tenant tests, handler tests, focused UI tests, and the clean browser journey cover the outcome. | Validate handoff wording and whether explicit next-owner semantics add value beyond current ownership and assigned account tasks with a pilot. |
| Customer activity reporting | `schema/API foundation` | Production-capable account touchpoint history, follow-up queue, health report, and period/teammate sales-activity report provide bounded source evidence, but no customer-only period report or persisted derived-health history exists | Complete the exact client rollup and period semantics in Phase 4 reporting convergence; never infer historical health changes from current derived state. |

## Communication, sales, and growth

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| System email | `production-capable` | Real Postmark adapter plus fake development default; verified-workspace, user-invite/setup, and password-reset templates; per-attempt provider message correlation; a hidden-until-configured constant-time Basic Auth Postmark callback; versioned shared-stream filtering; durable duplicate/tamper detection; exact user/tenant/email/key matching; complaint suppression; bounded 400-day secret-safe retention; UI guidance; and aggregate bounce/complaint/unapplied metrics and alerts. Callback storage excludes recipients, subject/body, raw delivery keys, and provider payloads. Fake links require explicit development/test mode and never appear in production/Postmark responses. | Configure the approved Postmark webhook credentials and run signup/invite/reset plus bounce/complaint through the live sandbox; retain provider and alert evidence. |
| Per-user send and mailbox sync | `production-capable` | Encrypted SMTP/IMAP credentials and encrypted Gmail/Graph OAuth tokens; scope-aware OAuth-only setup; Gmail base64url MIME and Microsoft Graph base64 MIME outbound adapters; Gmail/Graph ingestion; advisory-lock serialized token refresh shared by send/sync; manual sync plus durable idempotent mailbox jobs; bounded provider metrics; actionable reconnect errors; opaque bounded RFC message identifiers; Gmail provider message/thread receipts; raw MIME ingestion for IMAP/Gmail/Microsoft; normalized reply and DSN/ARF evidence; scanner-safe signed unsubscribe confirmation; bounded HTTPS RFC 8058 headers through every outbound adapter; and exact HTTP, UI, handler, MIME-injection, header-normalization, and disposable-PostgreSQL concurrency/encryption/tenant acceptance. Provider requests are never automatically retried after an ambiguous send boundary. | Run controlled send/sync/refresh/reply/bounce/complaint/unsubscribe acceptance with approved live Google Workspace and Microsoft 365 test mailboxes; retain raw received headers proving provider preservation and DKIM coverage of both one-click headers, plus provider evidence and richer revoked-consent recovery. |
| Mailbox, tracking, and shared inbox | `schema/API foundation` | Message log/detail, private-by-default inbound mail, reversible audited sharing, assignment/status, and chronological threaded replies now operate in Mailbox and Team Inbox. Replies always use the acting teammate's own connected mailbox—never the receiving mailbox owner's identity—inherit source privacy, preserve normalized RFC/provider thread evidence, and leave engagement tracking off. A request-key/hash ledger commits an immutable intent before SMTP/Gmail/Microsoft, claims once, rechecks sender identity and suppression immediately before effect, records provider acceptance and the outbound thread message atomically, and never automatically retries. Ambiguous provider/finalization outcomes and claims interrupted for five minutes become explicit `uncertain` items; only the original sender can retry, while owners/admins can confirm sent or mark not sent after checking the Sent folder. A one-minute multi-instance-safe recovery pass, aggregate privacy-safe gauges, validated alerts, and a runbook cover stuck/uncertain work. Existing shared-inbox row locking/version/privacy/audit controls and the explicit default-off 90-day engagement policy/retention pass remain. Handler/MIME/UI/metrics/migration tests plus disposable-PostgreSQL tenant/role/replay/claim/finalize/recovery/correlation acceptance cover the local contract; exact-correlated bounce/complaint suppression remains available. | Retain approved Gmail, Microsoft, and SMTP send/thread/ambiguous-recovery plus feedback/tracking evidence; validate send-as, privacy, consent, and retention wording with a pilot; then run the full pilot before promotion. |
| Email templates/snippets | `production-capable` | Organization CRUD, merge catalog/rendering, record composer integration | Add preview/test-send and custom-field merge support. |
| Email sequences | `schema/API foundation` | Draft-only authoring has admin-only audited approval bound to the exact content revision; migration 87 pauses legacy active definitions. Only current-revision-approved active sequences can enroll or reach the provider boundary, while any writer can issue an idempotent safety pause. Active definitions cannot be edited/deleted and enrollment history makes content immutable. Migration 88 classifies future cadence-finished, replied, and suppressed exits without guessing history. Migration 89 stores a stable RFC `Message-ID` before the provider boundary and qualifies replies only through exact header references or one unambiguous provider thread under tenant/contact/mailbox/time checks. Migration 90 ingests only machine-readable terminal RFC 3464 DSNs and RFC 5965 ARF complaints, then requires exact tenant, enrolling mailbox, opaque message ID, post-acceptance time, and non-conflicting recipient correlation. Matched feedback retains an independent bounce/complaint outcome, suppresses the recipient, exits active/paused cadences, links evidence, and audits; complaint takes precedence. Signed unsubscribe links use read-only scanner-safe GET confirmation, exact idempotent RFC 8058 POST, monotonic suppression evidence, text/HTML fallback, and common SMTP/Gmail/Microsoft one-click headers when the public URL is HTTPS. Missing, ambiguous, wrong-tenant/mailbox/recipient, temporary, and unmatched reports remain unapplied without changing delivery state. Correlation identifiers are excluded from portable exports. Managed Stripe runtimes atomically reserve shared PostgreSQL budgets of 1,000 eligible attempts per tenant per 24-hour fixed window and 100 per sender per one-hour fixed window by default; exhaustion defers the job without spending an attempt or claiming delivery, configuration is fail-fast, identifiers remain hashed, and self-hosted billing has no Open CRM hosted cap. Listings/UI distinguish provider acceptance from later bounce/complaint, reply, finish, suppression, cancellation, queued, and uncertain-review outcomes. Transactional enqueue, leased jobs, immutable snapshots, stale-`sending` quarantine, and audited manual resolution remain covered. Handler/UI/unit and disposable-PostgreSQL tests cover roles, revision invalidation, races, caps, exact/ambiguous reply and feedback attribution, duplicates, suppression, precedence, direct mail, recovery, and tenant isolation. | Retain approved real-provider send/reply/bounce/complaint/refresh/unsubscribe evidence, including raw delivered MIME and DKIM coverage of both one-click headers; validate provider-specific deliverability behavior and provisional caps, then run a pilot before promotion. |
| Calls and SMS | `usable with fake provider` | Persistent history, consent/opt-out metadata, and fake providers remain available for development. Their contact actions are omitted from the production journey and guarded out of the production bundle so fake sends/calls are not presented to pilots. | Implement real carrier calls/webhooks/delivery receipts, media retention, compliance, and sandbox tests before restoring production navigation. |
| Meetings and booking links | `usable with fake provider` | Events, availability, transactionally enqueued idempotent in-app reminders, booking-link definitions, and a fake calendar provider remain available for development. Contact meeting actions and booking-link management are omitted from the production journey and guarded out of the production bundle because public slot booking/provider execution is absent. | Add public slot booking, conflict checks, round robin, real calendar providers/webhooks, and guest notifications before restoring production navigation. |
| Pipeline configuration | `production-capable` | Admin UI and tenant-scoped serializable APIs create/rename/default up to 10 pipelines, create/rename/reclassify/reorder up to 20 unique stages, configure bounded open-stage probabilities while fixing won/lost at 100/0, retain stage IDs across edits, prevent outcome reclassification while any active or archived deal uses the stage, and emit audit history; role, conflict, cross-tenant, disabled-actor, PostgreSQL, UI, and browser continuity tests cover the outcome | Validate terminology and practical stage limits with a pilot; deletion remains intentionally absent because safe replacement/migration semantics are not yet specified. |
| Deal probability and forecasting | `production-capable` | Configurable audited stage probability drives period-bounded team, owner, unassigned, and stage rollups; unweighted/won/weighted values, close-date semantics, manual-FX omissions, and assumptions are explicit. Deal list/saved views/CSV share validated close-date filters. Unit, handler, disposable-PostgreSQL tenant/period/probability acceptance, focused UI, and clean-browser evidence cover the vertical flow. | Validate probability defaults, no-close-date policy, and quota operating cadence with a pilot; add immutable forecast snapshots only if pilot review requires point-in-time comparisons. |
| Sales activity reporting | `production-capable` | A viewer-safe live report counts deal creation/moves/won/lost, notes, and task creation/completion over a bounded UTC window with retained-teammate filtering, precise owner-vs-actor semantics, outcome win rate, stage entry/exit math, grouped won/lost reasons, retained close notes, and deal deep links. Transactional event-time snapshots preserve deal/pipeline/stage/outcome/reason meaning after edits; stage-ledger and close-reason coverage windows are disclosed separately. Handler, UI, clean-browser, and disposable-PostgreSQL snapshot/math/disabled-user/activity-link/cross-tenant acceptance cover the outcome; a mixed-history planner gate requires tenant/date and owner/date stage-event/activity indexes and excludes unrelated audit actions before aggregation. | Validate the default window, reason vocabulary, UTC-day presentation, rate definitions, and teammate usefulness with a pilot; add cohort/velocity metrics only with explicit event-time definitions. |
| Deal task automation rules | `production-capable` | Admin-managed tenant rules create exactly one bounded task on deal creation, a real stage change (optionally to one stage), or direct/bulk archive. Task, activity, idempotent run, and audit commit with the deal event; assignment uses the active deal owner or trigger-actor fallback. Broad unsupported definitions are hidden and safely skipped. UI/handler and disposable-PostgreSQL trigger/replay/tenant tests plus the clean-browser pilot journey cover the outcome. | Validate the three triggers, task wording, due-day range, and owner fallback with a pilot; keep broader workflow actions hidden until their own complete runtime slices. |
| Lead follow-up task automation | `production-capable` | Owners/admins configure one exact active lead form or every active form, one optional attribution condition, an exact active teammate, literal task content, a 0–365 whole-day creation delay, and a separate 0–365-day due offset measured from task creation. Accepted public submissions snapshot the executable definition and transactionally retain one immutable run schedule plus a tenant-scoped durable job with the same `run_at`. Future jobs cannot be claimed early, do not trigger false stalled alerts, and do not cause browser hot-polling. The worker rehydrates retained source records, rechecks activation and membership, evaluates the snapshot, and atomically records task/reminders, assignment notification, activity, audit, and terminal run evidence; legacy rules retain their original immediate-creation/due-offset semantics until edited. Stable run/job/task identity makes capture retry and lost acknowledgement safe; hosted suspension defers through the shared billing guard; deactivation or a spam review cancels queued effects; recovery creates only one lineage-preserving successor from the retained snapshot; mismatches skip; inactive assignees fail visibly. UI/unit/disposable-PostgreSQL schedule-boundary, replay, edit-snapshot, review/recovery, negative-path, and cross-tenant tests plus the clean-browser journey cover the local outcome. | Validate rule language, review window, attribution fields, creation/due timing, assignment practice, and failure noise with a pilot before using this as automatic lead qualification or routing. |
| Catalog, line items, and quote versions | `production-capable` | Tenant-scoped catalog/custom items snapshot saved name/SKU/type/price and calculate discount/tax/total. The authenticated current-data PDF is explicitly a mutable draft. Finalization transactionally locks the live deal, allocates a per-deal version, snapshots organization/deal/recipient/preparer/validity/terms/line items/totals, and stores the exact PDF bytes plus SHA-256. Actor-scoped hashed idempotency makes identical sequential/concurrent retries return one version and rejects changed-payload key reuse. An exact active template revision can supply terms, validity, delivery/signature defaults, and review policy; finalization rejects revision/terms drift and snapshots the exact reference and rendered defaults. A writer, template, or workspace review requirement creates one pending decision bound to the stored PDF digest. A different active owner/admin—not the requester or creator—makes an idempotent terminal decision; pending/rejected versions fail at delivery preparation and provider claim, while reissue retains the template revision and requests fresh review. Every new document also locks and retains the workspace base currency plus an identity or newest effective tenant/currency-pair rate, source, and rounded reporting total; missing/future/foreign rates fail before evidence exists, while PDF/staff/customer/export/audit views disclose the same immutable snapshot without changing the quote-currency amount due. A writer can then deliver that exact version through the connected SMTP/Google/Microsoft mailbox: Open CRM persists the intent and stable RFC Message-ID first, rechecks approval, active membership, suppression, and sender identity, records provider acceptance with shared email/activity/audit evidence, quarantines ambiguous sends without automatic retry, and offers role-safe explicit recovery. Every expired version is visibly non-deliverable. A writer can create exactly one tenant/deal-bound immutable replacement from an expired unsigned version; one transaction retains the original bytes/evidence, copies commercial content into a new PDF/version/digest, snapshots the replacement-date FX disclosure, voids an expired pending signature, and rejects signed, unresolved, active, cross-tenant, repeated, and changed-idempotency paths. Digest-only expiring public links expose the stored PDF with approximate view/download counters and a distinct idempotent receipt action that explicitly is not approval or signature. Startup/minutely recovery, bounded delivery/approval/signature metrics and alerts, shared public abuse budgets, no-store/no-referrer responses, portable template/approval/delivery/reissue/FX evidence without tokens/correlation hashes, handler/UI/migration/disposable-PostgreSQL revision/self-review/race/recovery/expiry/FX/tenant tests, and the two-admin real-SMTP PostgreSQL browser journey with blocked send, exact-PDF approval, and customer/staff WCAG scans cover this boundary. | Validate accounting policy, delivery/reissue expectations, approved live mailbox providers, and a pilot. |
| Proposal tracking and e-signature | `schema/API foundation` | The production first-party path creates a recipient-specific request only while delivering one immutable quote with retained effective-dated FX and exact preparation-template disclosure, activates it only after mailbox-provider acceptance, and makes definite failure/deactivation voiding plus ambiguous-send recovery atomic with delivery state. Required independent approval binds a different active owner/admin's terminal decision to the exact retained PDF digest before delivery can reach the mailbox boundary. The bearer-link recipient must type the expected name and explicitly accept the retained consent statement before a one-time, digest-idempotent terminal decision. Signed evidence binds expected email, typed name, consent/time, quote PDF SHA-256, and a retained certificate PDF/SHA; staff can download or void an unsigned request but cannot mark it signed. Expired unsigned versions have an explicit immutable reissue/replacement path; signed evidence and unresolved sends block it. A writer can deliberately convert signed native evidence into a selected same-tenant won stage and required close review; one transaction binds certificate, activity/event, actor, automation, outcome, and client handoff. Exact replay is harmless even after deliberate reopening, while changed reuse and terminal reuse fail closed. Concurrency, cross-tenant, role, portable-evidence redaction, aggregate approval/awaiting-conversion/converted metrics, UI accessibility, and PostgreSQL/browser acceptance cover the local outcome. Historical manual rows are read-only and labeled non-evidence. | Retain accounting-policy, approved live-mailbox, jurisdiction/consent, and pilot evidence before promotion. An external signing provider is optional only if pilots require identity assurance or jurisdiction coverage beyond the first-party email-link contract. |
| Lead capture forms/pages/widgets | `schema/API foundation` | Admins configure the exact consent statement shown on embeds, hosted pages, and widgets. Every write requires an organization/form-bound, digest-only, two-second-delayed one-time challenge with a 30-minute lifetime; acceptance re-locks the active form and challenge, snapshots the consent statement/time, creates the contact/activity/submission, and consumes the challenge in one transaction. Exact request retries return the original effect while changed, expired, premature, cross-form, and cross-tenant attempts fail closed. New submissions retain no raw client address or user agent. Owners/admins have a bounded review queue with retained payload/attribution/consent context. Marking spam atomically archives only the captured contact and cancels queued follow-up work; completed tasks remain history. Digest-idempotent recovery restores only the exact quarantine archive and creates at most one successor run from retained evidence, while the generic archive path cannot bypass the state. Aggregate state/age metrics and alerts, audit/activity evidence, handler/UI/disposable-PostgreSQL lifecycle/tenant tests, and the Chromium capture/quarantine/recovery/assignment plus public WCAG journey cover this boundary. | Add an approved edge/WAF/reputation layer, configurable custom-field mapping, conversion analytics, custom domains/SEO, automatic scoring/routing, and pilot evidence before promotion. |
| Audiences and lead scoring | `schema/API foundation` | Saved segment counts and manually invoked rule evaluation/routing remain testable in development. Contact-scoped evaluation state prevents duplicate in-flight work, validates the returned contact identity, and rejects stale results even after leave-and-return navigation. Their management routes are absent from production navigation and bundles because audience activation and automatic scoring/routing do not yet perform the promised end-to-end job. | Add member drill-down, automatic scoring, simulation, exclusions, bulk rescore, SLA/round robin, observability, and acceptance tests before production exposure. |
| Marketing and nurture campaigns | `schema/API foundation` | Definitions, schedule metadata, audience snapshots, and sequence binding remain testable in development, while their management routes are absent from production navigation and bundles because no compliant delivery runtime exists. | Add approvals, recipient expansion, durable compliant delivery, unsubscribe enforcement, exits/replies, analytics, and acceptance tests before production exposure. |

## Automation, analytics, and operations

| Capability | Maturity | Current evidence | Required next outcome |
| --- | --- | --- | --- |
| General workflow definitions | `schema/API foundation` | Broad trigger/condition/action/timing/approval definitions and run metadata remain in tenant-scoped storage/APIs. Normal navigation exposes only two executable one-task shapes: deal events and a lead-form submission with one optional attribution condition, active assignee, and bounded whole-day creation/due timing. Unsupported definitions remain hidden. | Keep broad definitions hidden until their own record hydration, event capture, nested branching UX, validation previews, and negative-path coverage are complete. |
| General workflow execution | `schema/API foundation` | Deal-event tasks execute transactionally with their source mutation. Lead-form tasks use an exact trigger snapshot, transactional enqueue, an immutable due schedule, the durable leased runner, source rehydration, condition evaluation, activation/membership guards, idempotent replay, audit/activity/run evidence, hosted-billing deferral, due-aware aggregate metrics, and alerts. Other stored action types, general schedules, approvals, targets, and nested branches are not dispatched. | Implement each broader family through the same source-guarded durable contract with loop protection, bounded retry/recovery, action attempts, audit, and acceptance tests before exposure. |
| Saved table reports | `production-capable` | Production Reports lets writers save/edit and all workspace members run active contact, company, deal, and task table reports; owners/admins can download the same saved query as CSV. A static source/field registry compiles only parameterized typed filters, grouping, and aggregation; every query binds the session tenant, excludes archived rows, and has a five-second deadline. Screen results cap page/page size at 100. CSV inspects row 10,001 and fails explicitly instead of truncating, includes a BOM, neutralizes spreadsheet formulas, is non-cacheable, and records a transactional audit event. Definition changes and export revalidate the active actor inside their transaction. Stable permission/inactive/unsupported/overflow/timeout errors, accessible responsive tables, latest-request cancellation, handler/UI tests, freshly migrated PostgreSQL all-source/archived/cross-tenant/formula/overflow/audit acceptance, and the two-workspace Chromium journey cover this bounded outcome. The CI-gated 12-tenant PostgreSQL performance test expands one workspace to 10,000 contacts, budgets a 100-row saved-report page at two seconds and the complete export at five seconds, proves foreign-workspace denial, then requires row 10,001 to fail without an extra download audit. | Validate report usefulness and practical limits with real pilot traffic; retain the checked page/export budgets and keep charts, dashboards, sharing, and scheduling hidden until separately complete. |
| Custom report visualizations | `schema/API foundation` | Bar, line, funnel, pie, and KPI metadata and development-only editor options remain stored and validated, but production navigation exposes only executable tables. There is no chart renderer, dashboard widget, sharing policy, or scheduled delivery claim. | Implement and promote one renderer/user outcome at a time with accessible alternatives, performance limits, sharing/permission rules, exports, and acceptance evidence. |
| Dashboards and scheduled reports | `not started` | Fixed operational dashboard only | Add configurable widgets, layouts, scheduled delivery, and performance strategy after report execution. |
| Durable background jobs/outbox | `production-capable` | Tenant-scoped PostgreSQL queue with idempotency keys, leased `SKIP LOCKED` claims, capped retries, dead letters, admin replay/health UI, aggregate queue/lag/outcome metrics, graceful worker, and real-PostgreSQL restart/duplicate tests; meeting/task reminders, mailbox sync, sequence sends, provider reconciliation, daily billing usage snapshots, workspace export generation, and lead follow-up task execution use it. Expected policy blocks have a distinct durable deferral path that releases the claim, preserves earlier genuine attempt counts, records the reason, schedules a later retry, and emits a bounded `deferred` outcome. Hourly multi-instance-safe retention compacts successful detail after 30 days and deletes successful rows after a 400-day idempotency window in bounded batches, while active and dead work remains untouched for recovery; only the eight explicitly reviewed source-guarded production job types are eligible. | Move future automation/campaign/import work onto the same runner, requiring a source-state duplicate guard and explicit retention allowlist review for effects beyond the documented successful-job window. |
| CI quality gates | `production-capable` | Exact Go 1.26.5/Alpine 3.24 and Node 24 toolchains, zero-reachable-vulnerability Go and zero-high-severity frontend audits, pinned zero-unsuppressed-finding `gosec` analysis with an executable guard requiring rule-specific explained exceptions, a reproducible license-policy inventory of all shipped Go/npm runtime components plus the complete installed npm development graph and freshness-gated `THIRD_PARTY_NOTICES.md`, real-PostgreSQL backend tests, isolated self-hosted and hosted-Stripe Chromium journeys, automated WCAG A/AA scans over critical public and authenticated surfaces with per-surface failure evidence, encrypted backup/restore acceptance, immutable deploy/rollback acceptance, migration compatibility policy, source-size no-growth ratchets with every production `internal/app` file below the default 500-line ceiling, package-wide route-inventory and hosted-write-policy scans across split registration files, validated Prometheus alert rules, retained browser failure evidence, and deploy dependency on all four jobs | Extend static, license, and accessibility coverage with each promoted Phase 2–4 journey and any new distribution artifact. |
| Deployment and rollback | `production-capable` | CI-gated immutable commit-tagged API image, exact public API release verification, exact frontend commit marker verified over HTTPS, expand/contract migration enforcement, atomic release manifests, production database-startup fail-fast behavior, readiness-based container health, a 45-second exact-release stabilization window, disposable database-unavailable boot recovery, tested automatic failed-readiness recovery, guarded manual application rollback, and operator runbook | Close `P3-O5` by enforcing a Cloudflare HTTP-to-HTTPS redirect without an origin loop; repeat automatic host/daemon recovery in an approved production window, define image retention, and require backup/restore evidence before the first approved contract migration. |
| Backups and restore | `production-capable` | Pinned Restic path creates and verifies custom dumps, encrypts snapshots, applies retention/checks, records atomic success/failure evidence, restores into isolated PostgreSQL, forward-migrates, sanity-checks, and gates deploys through CI; daily/weekly systemd templates reject local production repositories | Provision approved off-host credentials, enable timers on the production host, connect freshness/failure alerts, and record a real-repository drill before pilot validation. |
| Public endpoint abuse controls | `production-capable` | Every credential, provisioning, public lead/page/widget, Stripe/Postmark webhook, unsubscribe, and email tracking surface has an explicit route budget or high-entropy authentication before its shared budget. Migration `079_shared_public_rate_limits.sql` makes fixed windows atomic across API processes and restarts, retains only scope plus a one-way client digest, clamps counters, opportunistically removes expired rows, returns exact `Retry-After`, and fails closed when PostgreSQL is unavailable. Public lead writes add a digest-only delayed one-time challenge, explicit consent, exact replay collapse, no new raw browser/network identifier retention, and bounded outcome alerts. A route-family policy matrix, authenticated Postmark boundary tests, concurrent PostgreSQL acceptance, privacy/reset tests, and browser coverage exercise the application boundary. | Add and validate an approved production edge/WAF/reputation layer; application controls are not a volumetric DDoS boundary. |
| Observability and alerting | `schema/API foundation` | Redacted structured route/request/job/provider logs; token-protected bounded-route HTTP latency/error, database, aggregate queue, worker, public abuse-decision, Postmark/SMTP/Gmail/Microsoft send and OAuth-refresh outcomes, connected-mailbox reply and finalized-quote delivery claims/uncertainty/recovery, workflow queued/running/failed/skipped and due-only oldest-active state, lead-review unreviewed/legitimate/spam state and oldest-unreviewed age, quote-approval pending/approved/rejected state and oldest-pending age, quote-signature awaiting/expired/signed/declined/voided/awaiting-conversion/converted state, system- and customer-email bounce/complaint/unapplied feedback, notification backlog/event-quality/retention, email-engagement cleanup, and backup/restore Prometheus metrics; promtool-validated alert rules; tenant job/reply/quote recovery UI; initial SLO/runbooks | Configure production scrape credentials and an approved Alertmanager destination, prove synthetic alerts plus backup/provider/customer-feedback/reply/workflow/lead-review/quote-approval/signature-recovery/retention incidents, then validate SLOs/retention against pilot traffic. |
| Performance and failure testing | `schema/API foundation` | Real-PostgreSQL gates seed 12 representative tenants for core read/write latency plus mixed multi-tenant sales event/audit history; they assert core and sales-report tenant/date and owner/date index plans, exact totals, 96 concurrent mixed reads, 32 transactional cross-tenant writes, wrong-tenant denial, bounded exhausted/locked/closed database failures and recovery, both core and saved-report tenant-isolated 10,000-row exports under 5 s, a 100-row saved-report page under 2 s, explicit row-10,001 refusal without false audit evidence, and a mapped/deduplicated 1,000-row import under 10 s. Postmark `503`/deadline recovery and ambiguous SMTP quarantine are tested; production builds enforce entry/lazy/total/CSS raw+gzip budgets. | Retain the same evidence from an approved production-like host run and add provider/feature loads as remaining Phase 3 outcomes converge. |
| Browser end-to-end tests | `production-capable` | One Playwright/Chromium CI run drives the self-hosted/unmanaged pilot journey: idempotent workspace bootstrap, mandatory owner-email verification and trial start, task-reminder preference persistence, invitation rotation with old-link rejection, revocation, reactivation, final activation, and later member deactivation/reactivation, manual active-sign-in revocation plus password-reset invalidation, required custom fields, public lead-form/landing-page configuration, a conditional durable lead follow-up task with run evidence, challenge-bound consent capture, populated-review WCAG scan, spam quarantine/contact hiding/completed-task retention and exact recovery, owner assignment, mapped import/rollback, client/contact creation and reviewed duplicate merge, pipeline probability/rename continuity and forecast, calculated line items, immutable finalization, real-SMTP sandbox signature delivery, captured-message/link evidence, isolated public lead and quote-ceremony WCAG scans, exact-name/consent signing, customer/staff certificate digest equality, deliberate signed-quote conversion with its staff WCAG scan, retained conversion evidence, won close review, deal task automation, transactional client handoff and account summary, receipt/access evidence, current-PDF download, reminders, touchpoints/follow-up, data quality, mentions/following, recurring renewal scheduling/dashboard visibility/next-task advancement, saved table report creation/execution/audited CSV download with WCAG, attachment/header checks, and cross-tenant result/export denial, fixed report/export reconciliation, reversible client bulk status, task archive/restore, all core exports, a durable full-workspace ZIP with checksum and cross-tenant denial, persistence, and two-context tenant/contact/quote/deal-close isolation against PostgreSQL 16. A second isolated PostgreSQL run uses the production Stripe adapter with a local HTTP sandbox and drives verified trial start, server-created Checkout/Portal navigation, signed exact-event replay, webhook-only activation, invoice/dunning visibility, writable grace, unpaid suspension and direct-write denial, recovery, scheduled/final cancellation, suspension-safe export, and WCAG scans. The self-hosted journey waits for durable mutation responses before dependent actions and deliberately has no retry because its functional and accessibility workspaces consume the exact production signup budget; this preserves the original failure instead of masking it behind a secondary rate-limit rejection. A separate axe-core journey rejects automated WCAG A/AA violations across critical public and authenticated surfaces including lead review and profile/session security, retains a structured report per surface, and verifies the keyboard skip-link entry point. | Extend the same journeys through approved live-provider/pilot evidence, broader automation, and richer reporting as those outcomes converge. |
| Frontend route loading/maintainability | `schema/API foundation` | Route-level lazy loading plus CI-gated entry, async, aggregate raw/gzip, hidden-foundation, and 500-line source budgets remain fail-closed. The production-URL build measures 178.92/54.78/687.01 KiB raw and 58.02/15.63/217.51 KiB gzip for entry/largest-async/aggregate assets under 190/60/690 raw and 65/16/220 gzip ceilings. The production saved-table report route remains 31.49/8.12 KiB while its orchestration is 298 lines and its pure tested catalog/form model is 209 lines. Its non-table controls and definitions are filtered from production navigation; audience/scoring, booking, and marketing management remain development-only routes rejected from production bundles. Public lead pages/widgets share one challenge/consent submission component, while the lead-form, public-quote, quote-template, and task-automation routes remain bounded. Contact/company/deal/task production routes remain 449/458/473/496 lines. Focused, tested hooks and workspace components retain abort, visit-identity, idempotency, pending-state, response-identity, loading-state mutation guards, and 24-pixel populated-record selection targets across record and quote mutations. | Preserve the no-exception source and measured bundle ratchets, keep the saved-report model split as reporting expands, and keep incomplete foundations out of production navigation. |

## Deferred categories

AI, help desk/tickets, marketplace/custom objects, native mobile, real-time collaboration, and enterprise breadth are outside convergence scope. They must not be exposed or started until pilot evidence changes `docs/product-vision.md` through an explicit product decision.
