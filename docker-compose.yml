services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: open_crm
      POSTGRES_USER: open_crm
      POSTGRES_PASSWORD: open_crm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U open_crm -d open_crm"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
